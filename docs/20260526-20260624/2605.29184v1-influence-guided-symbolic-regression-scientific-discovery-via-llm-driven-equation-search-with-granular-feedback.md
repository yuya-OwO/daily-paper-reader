---
title: "Influence-Guided Symbolic Regression: Scientific Discovery via LLM-Driven Equation Search with Granular Feedback"
title_zh: 影响引导的符号回归：基于LLM驱动的方程搜索与细粒度反馈的科学发现
authors: "Evgeny S. Saveliev, Samuel Holt, Nabeel Seedat, David L. Bentley, Jim Weatherall, Mihaela van der Schaar"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.29184v1"
tags: ["query:sr"]
score: 10.0
evidence: 大语言模型驱动的符号回归与细粒度反馈
tldr: 符号回归面临搜索效率低和反馈信号粗糙的问题，现有方法仅使用全局均方误差引导，无法定位具体项的错误来源。本文提出影响引导符号回归（IGSR），将方程发现分解为两步迭代过程：LLM生成候选基函数，然后基于每个项对泛化精度的边际贡献（影响分数）进行剪枝。结合蒙特卡洛树搜索平衡探索与利用，在多个基准（LLM-SRBench、药理学模型、流行病学模拟、基因组数据）上表现优异。尤其在高维生物数据案例中，IGSR发现了DNA甲基化与RNA聚合酶II暂停的新关联，并经湿实验验证，展现了真实科学发现能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29184v1/fig-001.webp\", \"caption\": \"\", \"page\": 49, \"index\": 1, \"width\": 1708, \"height\": 1285}, {\"url\": \"assets/figures/arxiv/2605.29184v1/fig-002.webp\", \"caption\": \"\", \"page\": 69, \"index\": 2, \"width\": 439, \"height\": 439}]"
motivation: 现有基于LLM的符号回归方法依赖标量指标（如全局MSE）作为反馈信号，无法精细识别方程中不同项对性能的影响，导致搜索效率低且易陷入局部最优。
method: IGSR以线性模型形式迭代搜索：LLM生成候选基函数，然后计算每个基函数的影响分数（边际贡献），据此进行剪枝，并集成到MCTS框架中实现高效搜索。
result: 在多个合成与真实基准上，IGSR显著优于基线方法；在真实基因组数据案例中，成功发现DNA甲基化与RNA聚合酶II暂停的新关系，并经湿实验验证。
conclusion: IGSR通过细粒度影响反馈引导LLM驱动的符号回归，提升了科学发现的效率和可靠性，有望加速从数据中自动发现可解释的数学规律。
---

## 摘要
大型语言模型（LLM）为科学发现提供了有前景的途径，但它们在符号回归中的应用常受到低效搜索策略和粗糙反馈信号的制约。当前方法通常使用标量指标（例如全局均方误差）来引导LLM，这无法识别所提出方程的哪些成分驱动了性能或导致了误差。我们引入了《影响引导的符号回归》（IGSR），该方法将方程发现建模为迭代的两步过程，结合多样化项生成与严格选择：LLM生成线性模型的候选基函数$ψ_j(\mathbf{x})$，然后使用细粒度影响分数$Δ_j$进行评估。这些分数量化了每个项对泛化准确性的边际贡献，从而实现影响引导的剪枝过程，系统地优化模型结构。将该机制集成到蒙特卡洛树搜索（MCTS）中，能够在组合搜索空间中导航，同时平衡对新函数形式的探索与高影响成分的利用。我们在多个基准测试上展示了IGSR的有效性，包括LLM-SRBench、药理学PKPD模型、一个流行病学模拟以及真实世界基因组数据。值得注意的是，我们使用一个高维生物数据集在案例研究中验证了该框架进行真正发现的能力，其中IGSR识别了DNA甲基化与RNA聚合酶II暂停之间的新关系；该假设随后通过湿实验得到了支持。

## Abstract
Large Language Models (LLMs) offer a promising avenue for scientific discovery, yet their application to symbolic regression is often constrained by inefficient search strategies and coarse feedback signals. Current methods typically guide LLMs using scalar metrics (e.g., global Mean Squared Error), which fail to identify which components of a proposed equation are driving performance or causing error. We introduce \textit{Influence-Guided Symbolic Regression} (IGSR), a method that frames equation discovery as an iterative two-step process combining diverse term generation with rigorous selection: an LLM generates candidate basis functions $ψ_j(\mathbf{x})$ for a linear model, which are then evaluated using granular influence scores $Δ_j$. These scores quantify each term's marginal contribution to generalization accuracy, enabling an influence-guided pruning process that systematically refines the model structure. Integrating this mechanism into a Monte Carlo Tree Search (MCTS) enables navigating the combinatorial search space while balancing exploration of novel functional forms with exploitation of high-influence components. We demonstrate IGSR's effectiveness on a diverse suite of benchmarks, including LLM-SRBench, pharmacological PKPD models, an epidemiological simulation, and real-world genomic data. Notably, we validate the framework's capacity for genuine discovery in a case study using a high-dimensional biological dataset, in which IGSR identified a novel relationship between DNA methylation and RNA Polymerase II pausing; a hypothesis that was subsequently supported via wet-lab experimentation.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **问题**：现有基于大语言模型（LLM）的符号回归方法通常使用全局标量指标（如均方误差 MSE）作为唯一反馈信号，无法区分方程中哪些具体项对性能有益或有害，导致搜索效率低下、容易陷入局部最优。
- **意义**：科学发现需要可解释、可泛化的数学方程，而传统符号回归（如遗传编程）在高维和复杂系统中效果有限。LLM 虽然能生成有科学直觉的候选函数，但缺乏细粒度的反馈机制来指导选择与剪枝。本文提出的 IGSR 通过引入**逐项影响分数**来解耦生成与选择，使搜索更高效、更可靠，并通过湿实验验证了其真正发现新科学知识的能力。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：将方程发现建模为“提出-剪枝”的迭代过程：LLM 生成候选基函数 $\psi_j(\mathbf{x})$，然后计算每个基函数对验证集 MSE 的边际贡献（影响分数 $\Delta_j$），据此保留或丢弃项。该过程嵌入蒙特卡洛树搜索（MCTS）中以平衡探索与利用。
- **关键技术细节**：
  - **模型形式**：目标为稀疏线性模型 $f(\mathbf{x}) = \sum_j w_j \psi_j(\mathbf{x})$，其中 $\psi_j$ 是 LLM 提出的非线性基函数。
  - **影响分数 $\Delta_j$**：定义为移除项 $j$（即设 $w_j=0$）后验证集 MSE 的增加量，反映该项的边际贡献。计算时不重新拟合模型（no-refit），高效且能避免过度拟合。
  - **剪枝策略**：默认采用确定性剪枝，即保留影响分数最高的 $K$ 个项（$K=6$）。变体 IGSR-Agent 使用第二个 LLM 进行语义剪枝。
  - **搜索策略**：MCTS 以节点表示方程，每次扩展执行一次“提出-剪枝”循环，奖励为负验证 MSE，UCT 准则选择子节点。
  - **历史反馈**：每次迭代记录保留/丢弃的项及 MSE 变化，作为下一个提出步骤的上下文，实现上下文内学习。

### 3. 实验设计

- **使用的数据集/场景**：
  - **六个生物/临床基准**：肺癌 PKPD（三个变体：无治疗、化疗、化放疗）、COVID-19 流行病模拟（Covasim）、RNA 聚合酶 II 暂停数据集（263 特征）、华法林药代动力学数据集。
  - **LLM-SRBench**：包含 128 个来自物理、生物、材料科学、化学的发现导向问题，旨在测试真正发现能力而非记忆。
  - **真实生物学案例**：基于 eNET-seq 数据预测 Pol II 暂停位点，引入 CpG 甲基化映射后提出假设，并通过 5-氮杂胞苷处理细胞进行湿实验验证。
- **对比方法**：
  - **白盒非 LLM**：SINDy、GPLearn、PySR
  - **白盒 LLM**：ZeroShot、ZeroOptim、ICL、D3-white-box、LLM-SR、LaSR、ICSR
  - **黑盒**：DyNODE、RNN、Transformer、XGBoost
  - **其他**：AutoFeat、OpenFE、SyMANTIC、CAAFE、LIES
- **评估指标**：MSE、NMSE、Acc 0.1、Term Recall、Symbolic Accuracy 等。

### 4. 资源与算力

- 文中未明确说明具体 GPU 型号、数量和训练时长。LLM 推理主要使用 Azure AI Foundry 的 serverless API 和 Azure VM（Standard NC96ads A100 v4，4×NVIDIA A100 80GB）。模型优化（线性拟合）在本地工作站（Intel i9-10900K，64GB RAM）或服务器（AMD EPYC 9V84，630GB RAM）上完成。
- 每轮 MCTS 扩展通常数秒至数分钟，完整运行可达数小时，但均不超过 3 小时/种子。对于高维 RNA 聚合酶数据集，峰值 RSS 约 2.8GB，内存需求合理。

### 5. 实验数量与充分性

- **实验数量**：
  - 6 个基准数据集：25 种子（LLM 方法）或 10 种子（非 LLM 基线）。
  - LLM-SRBench：128 个问题，5 种子/问题。
  - 消融实验：MCTS 与影响反馈的消融（8 种变体）、MCTS 展开深度、提议项数、提示变化、LLM 骨干网络（9 种）、特征数量鲁棒性、无关特征鲁棒性、合成模型基准等。
  - 湿实验验证：两个生物学重复，统计分析（Wilcoxon 检验）。
- **充分性**：实验设计较为全面，覆盖了多种类型的数据（合成、真实、高维、低维）、多种基线（传统 SR、LLM 方法、黑盒、自动化特征工程）、多种消融。但是，实验中默认使用 GPT-4o 和 GPT-4o-mini，可能对 LLM 版本敏感；部分方法（如 SINDy、PySR）在 RNA 聚合酶数据上无法运行（超时或内存溢出），对比不完全公平。

### 6. 主要结论与发现

- IGSR 在 6 个基准测试中平均排名第一（表 2a），显著优于所有白盒基线，且与黑盒模型竞争。
- 在 LLM-SRBench 上，IGSR 在预测性能（NMSE、Acc 0.1）和符号恢复（Term Recall、Symbolic Accuracy）上均优于其他 LLM 方法（表 2b）。
- **科学发现案例**：IGSR 从高维 RNA 聚合酶数据中发现了 DNA 甲基化抑制暂停的新假设；湿实验验证表明，去除甲基化后暂停显著增加，支持了该假设。
- 消融实验表明：**影响反馈**和**MCTS 搜索**都是关键组件，缺一不可。
- 方法具有良好的跨种子稳定性、对无关特征的鲁棒性，以及对不同 LLM 骨干的鲁棒性。

### 7. 优点

- **细粒度反馈机制**：逐项影响分数提供结构化的信用分配，比单一标量损失更有效地引导搜索。
- **解耦生成与选择**：LLM 发挥创造力产生候选，而影响分数驱动数据驱动的剪枝，减少幻觉和冗余。
- **可扩展性**：在高维（263 特征）下运行时间呈亚线性增长，避免了传统符号回归的组合爆炸。
- **验证真实科学发现**：通过湿实验验证了 AI 生成的假设，展示了从数据到假设到实验的完整闭环。
- **全面的实验评估**：包含多种基准、消融、鲁棒性测试和生物学案例，结论可靠。

### 8. 不足与局限

- **LLM 先验限制**：所需函数形式若不在 LLM 预训练分布中，则无法提出，限制了一定范围内的发现。
- **模型形式限制**：假设 $f(\mathbf{x}) = \sum w_j \psi_j(\mathbf{x})$，对高度非线性或嵌套动态系统（如 ODE）发现能力有限。虽然 IGSR-TLO 扩展了内部参数优化，但未在主要实验中系统评估。
- **计算成本仍然较高**：尤其是 MCTS 扩展和多次 LLM 调用，在资源受限场景下可能不可行。
- **实验覆盖有限**：仅使用 LLM-SRBench 的 128 个问题（非全部 239），且仅在部分数据集上对比了自动化特征工程方法（OpenFE 等在 RNA 聚合酶上超时）。材料科学领域符号恢复较差，表明对某些领域依赖近似。
- **超参数敏感性**：提示工程（如提案数、剪枝策略）对性能有影响，实验中仅探索了有限范围。
- **湿实验验证的假设**：仅在一个细胞系（HCT116）和一个药物（5-氮杂胞苷）上验证，普适性需更多实验。
- **过拟合风险**：虽然嵌套验证分析显示无显著过拟合，但 MCTS 和剪枝共用同一验证集，理论风险仍然存在。

（完）
