---
title: Learning Parametric Nitrogen Fertilizer Response Curves Using Neuro Symbolic Regression
title_zh: 使用神经符号回归学习参数化氮肥响应曲线
authors: "Giorgio Morales, John Sheppard"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31276v1"
tags: ["query:sr"]
score: 9.0
evidence: 使用transformer的神经符号回归方法发现函数
tldr: 精准农业中准确建模作物对氮肥的响应至关重要，但现有方法依赖预定义函数或黑箱模型。本文提出神经符号回归方法，利用基于Transformer的多集合符号骨架预测策略，无需预设形式即可学习参数化氮响应曲线。在合成数据和真实冬小麦数据上的实验表明，该方法能发现不同管理区的独特函数关系，拟合误差低于传统二次平台和指数模型。研究展示了神经符号回归在发现场地特异性农学关系、支持精准施肥决策方面的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31276v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 707, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 267, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 838, \"height\": 959, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 854, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 923, \"height\": 697, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.31276v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 930, \"height\": 650, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31276v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 916, \"height\": 497, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31276v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 776, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31276v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 660, \"height\": 329, \"label\": \"Table\"}]"
motivation: 现有氮响应曲线建模依赖固定函数形式或不可解释的机器学习，难以发现场地特异性关系。
method: 结合Transformer的多集合符号骨架预测与遗传算法拟合，从不同管理区子集推断共享函数结构。
result: 在合成数据中即使数据稀缺也能恢复正确表达式；在冬小麦数据中，发现的曲线拟合误差低于传统模型且捕捉空间多样性。
conclusion: 神经符号回归能自动发现可解释的氮响应函数，为精准农业提供数据驱动的决策支持。
---

## 摘要
精确模拟作物对氮肥施用的响应是精准农业中的一个基本挑战，因为它既影响经济回报又影响环境可持续性。现有方法要么依赖于预定义的参数化形式，要么依赖于不透明的机器学习模型，这限制了它们从数据中解释或发现地块特定函数关系的能力。在本工作中，我们提出了一种神经符号回归方法，用于学习参数化的氮响应曲线，而无需假设预定义的函数形式。我们的方法集成了基于Transformer的多集符号骨架预测策略，从而能够发现跨多个子区域或管理区的共享函数结构。通过构建多样的输入子集并在它们之间强制执行一致性，该方法恢复了稳健的符号骨架，随后使用遗传算法将这些骨架拟合到观测数据。该框架首先在合成的一维问题上进行了评估，以评估其在不同程度的认知不确定性下的稳健性。结果表明，即使在数据稀缺的情况下，所提出的神经符号回归方法也能恢复正确的表达式。在本工作中，我们展示了将该方法应用于真实冬小麦数据的结果，为田块内的不同管理区学习了不同的参数化氮响应曲线。结果表明，所发现的表达式不仅比传统模型（如二次平台和指数函数）实现了更低的拟合误差，而且捕获了跨空间区域的多样化函数行为。这证明了神经符号回归在发现地块特定农学关系和支持精准农业中知情决策方面的潜力。

## Abstract
Accurately modeling crop response to Nitrogen (N) fertilization is a fundamental challenge in precision agriculture, as it impacts both economic returns and environmental sustainability. Existing approaches either rely on predefined parametric forms or opaque machine learning models, limiting their ability to interpret or discover site-specific functional relationships from data. In this work, we propose a neuro symbolic regression (SR) approach to learn parametric N-response curves without assuming a predefined functional form. Our approach integrates a transformer-based Multi-Set Symbolic Skeleton Prediction strategy, enabling the discovery of shared functional structures across multiple subdomains or management zones (MZs). By constructing diverse input subsets and enforcing consistency across them, the method recovers robust symbolic skeletons that are subsequently fitted to observed data using a genetic algorithm. This framework was first evaluated on synthetic one-dimensional problems to assess its robustness under varying levels of epistemic uncertainty. The results demonstrate the ability of the proposed SR approach to recover correct expressions even in data-scarce regimes. In this work, we present the results of applying our method to real-world winter wheat data, learning distinct parametric N-response curves for different MZs within a field. The results show that the discovered expressions not only achieve lower fitting errors than traditional models such as quadratic-plateau and exponential functions, but also capture diverse functional behaviors across spatial regions. This demonstrates the potential that neuro SR has to enable the discovery of site-specific agronomic relationships and support informed decision-making in precision agriculture.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
精准农业中，准确建模作物对氮肥施用的响应曲线（N-response curve）对于优化施肥量、提高经济效益和减少环境危害至关重要。传统方法要么使用固定的参数化形式（如二次-平台模型、指数模型），这些形式过于刚性，难以适应田块内的空间异质性；要么使用黑箱机器学习模型（如神经网络），虽然拟合能力强但缺乏可解释性，无法揭示氮响应背后的数学规律。因此，**核心问题是如何自动从数据中发现可解释的、场地特异性的氮响应函数形式**。本文提出使用神经符号回归（Neuro Symbolic Regression, SR）解决该问题，旨在生成简洁、可解释的数学表达式，同时保持与神经网络相当的预测精度。

## 2. 方法论
### 核心思想
基于SeTGAP框架（Transformer + 遗传算法/遗传编程的符号回归），采用**多集合符号骨架预测**（Multi-Set Symbolic Skeleton Prediction, MSSP）策略。通过构建多个输入子集（或子域），强制要求这些子集共享相同的符号骨架（即函数形式），从而从不同区域或不同曲线中恢复稳健的函数结构，最后用遗传算法拟合系数。

### 关键技术细节
- **针对一维问题**：给定一个已知函数或模型预测曲线，不是直接复制数据集，而是从输入域中随机采样多个子区间（每个子区间80%~100%长度），每个子区间生成一个输入-输出对（若点数不足则插值）。这些子集构成集合D，送入预训练的Multi-Set Transformer，输出候选符号骨架（如`c1 + c2*tanh(c3 + c4*x)`）。重复ncand次，收集多样化候选，去重后用遗传算法拟合系数，选择MSE最小的表达式。
- **针对管理区（MZ）的氮响应曲线**：假设同一MZ内所有田块点的N-response曲线共享同一函数形式。从该MZ的所有曲线中随机选择NS条，每条曲线再随机采样子区间，形成NS个不同来源的子集，输入Transformer预测骨架。重复ncand次，用另一组随机曲线测试拟合误差，选择最优骨架。最终对MZ内每一条曲线独立拟合系数，获得特定场地的具体表达式。
- **算法流程**：详见原文Algorithm 1（一维问题）和Algorithm 2（MZ曲线骨架生成）。关键超参数：NS=10，n=3000，ncand=5，beam size nB=3。

## 3. 实验设计
### 使用数据集/场景
- **合成数据**：三个异方差噪声函数：`cos`（`f(x)=10-5*sin(x-5.86)`）、`hetero`（`f(x)=7.2*sin(x-18.8)`）、`cosqr`（`f(x)=sin(0.2*x^2+1.6)`）。初始数据集稀疏，通过自适应采样（ASPINN）逐步增加5个样本，共50次迭代。
- **真实数据**：一个冬小麦田（Field A），已有工作[13]将田块划分为4个管理区（MZ1-4）。每个MZ内各站点已有NN生成的N-response曲线（氮率0-150 lbs/acre，标准化后范围[-10,10]）。使用这些曲线作为“真实”进行骨架发现和拟合。

### Benchmark与对比方法
- 传统N-response模型：**二次-平台模型**（`c1 + c2*(min(x, c3) + c3)^2`）和**指数模型**（`c1*(1-exp(c2 + c3*x)) + c4`）。
- 对比指标：每个MZ内所有曲线拟合的平均绝对误差（¯r）。本文方法（SeTGAP）发现的不同函数形式（如tanh、cos组合、exp组合）与两种传统模型对比。

### 没有与其他SR方法比较
论文未与常见的遗传编程SR（如Eureqa、PySR）或深度SR方法（如DSR、SymbolicGPT）进行对比，仅与固定形式的传统农学模型比较。

## 4. 资源与算力
文中**未明确说明**使用的GPU型号、数量、训练时长等信息。仅提到Multi-Set Transformer是预训练的（参考[11]），但未提供其训练资源和时间。一维实验运行在ASPINN框架下，每次迭代执行Algorithm 1，但未报告计算时间。总体而言，算力细节缺失。

## 5. 实验数量与充分性
### 实验数量
- **合成数据**：3个函数，每个函数在50次自适应采样迭代中共执行50次符号回归（每次ncand=5）。展示了迭代1,5,10,15,20,25,30,35,40,45,50的表达式和MSE（表I），以及cosqr的拟合曲线图（图3）。
- **真实数据**：4个管理区，每个区产生一个最优骨架（表II），然后对区内所有曲线拟合系数，报告平均误差（¯r）。并展示每个MZ的2条示例曲线（图4）和所有曲线对比（图5）。
- **没有额外的消融实验**：未对超参数（NS、n、ncand）进行敏感性分析，也未在更多田块或作物上验证。

### 充分性与公平性
- **优点**：合成实验系统展示了不同数据密度下的恢复能力，证明了多集合策略的有效性；真实数据对比了两种常用传统模型，结果更优。
- **不足**：实验范围有限，仅1个农场数据，4个MZ，未与其他SR方法（如GP、DGSR）对比，公平性存疑。未在多个田块或跨作物上验证泛化性。消融实验缺失，难以判断各组件贡献。

## 6. 主要结论与发现
- 在合成数据上，即使初始数据稀疏，多集合策略能在约20次迭代后正确恢复目标函数的符号形式，且对局部不确定性具有鲁棒性。
- 在真实冬小麦数据上，不同MZ自动发现不同的函数结构（MZ1: `c1+c2*tanh(c3+c4*x)`; MZ2: 同MZ1; MZ3: `c1+c2*x+c3*cos(c4+c5*x)`; MZ4: `c1+c2*exp(c3*sin(c4+c5*x))`）。这些表达式的拟合平均误差（¯r为0.0355~0.0683）均低于二次-平台（0.0615~0.1028）和指数模型（0.1825~0.2303），表明能更准确地刻画空间异质性。
- 该方法为精准农业提供了一种数据驱动、可解释的氮响应曲线建模工具，可用于经济最优氮率计算和下游多目标优化。

## 7. 优点
- **无需预定义函数形式**：完全从数据中自动发现符号骨架，适应不同田间区域的曲率变化。
- **可解释性强**：输出简洁的数学公式，有助于农学家理解变量间的定量关系。
- **对认知不确定性稳健**：多集合采样策略在数据稀疏或局部失真时仍能恢复正确结构。
- **应用价值明确**：所得表达式轻量，可替代神经网络用于高效优化，降低计算成本。

## 8. 不足与局限
- **实验覆盖不足**：仅一个农场的四个MZ，未在不同土壤类型、作物或气候条件下验证；缺乏与现有SR方法的基准对比（如Eureqa、PySR、DSR）。
- **方法论局限**：作者自承是“ad hoc解决方案”，尚缺乏通用化、形式化的理论支撑；目前仅限于一维曲线（氮率-产量），未扩展到多变量场景。
- **消融缺失**：未分析NS、ncand等超参数的影响，也未做组件消融（如有无多集合策略、有无Transformer预训练）。
- **计算效率与可复现性**：未报告训练时间和资源消耗，预训练Transformer细节未公开，影响可复现性。
- **潜在偏差风险**：真实数据中的N-response曲线本身来自另一个NN模型，可能存在模型偏差；符号回归发现的骨架可能过拟合该NN误差而非真实物理规律。

（完）
