---
title: Contraction Analysis of Time-Delay Systems
title_zh: 时滞系统的收缩分析
authors: "Rintaro Watanabe, Yu Kawano"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2606.00179v1"
tags: ["query:lyapunov-ai"]
score: 9.0
evidence: 时延系统的李雅普诺夫-克拉索夫斯基泛函；收缩分析
tldr: 时滞系统的稳定性分析是控制理论中的难题。本文将收缩分析扩展至非线性时滞系统，在微分框架下引入Lyapunov-Krasovskii泛函和Razumikhin函数。证明了这些泛函的存在性等价于增量Lyapunov-Krasovskii泛函存在，且保证了一致增量指数稳定性。进一步，利用线性矩阵不等式（LMI）给出了单时滞系统的反馈控制器设计方法。该工作为非线性时滞系统提供了新的稳定性分析工具和可实现的控制器综合方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00179v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 416, \"label\": \"Figure\"}]"
motivation: 现有收缩分析主要针对无时滞系统，难以处理时滞系统的稳定性与控制问题，亟需扩展至时滞情形。
method: 在微分框架下扩展Lyapunov-Krasovskii泛函和Razumikhin函数，并利用线性矩阵不等式（LMI）设计反馈控制器。
result: 证明了微分泛函存在性等价于一致增量指数稳定性，并给出了单时滞系统的LMI控制器设计条件。
conclusion: 该研究为非线性时滞系统提供系统化的收缩分析框架和可实现的控制器设计方法。
---

## 摘要
在本文中，我们研究了由泛函微分方程描述的非线性时滞系统的收缩分析。我们首先在微分框架内扩展了Lyapunov-Krasovskii泛函的概念。然后我们证明其存在等价于一个增量Lyapunov-Krasovskii泛函的存在，并保证一致增量指数稳定性。接下来，我们在微分框架内扩展了Lyapunov-Razumikhin函数的概念，其存在也确保了一致增量指数稳定性。作为我们结果的应用，我们利用线性矩阵不等式为具有单时滞的非线性时滞系统制定了镇定反馈控制设计。

## Abstract
In this paper, we investigate contraction analysis for nonlinear time-delay systems described by functional differential equations. We first extend the concept of Lyapunov-Krasovskii functionals within the differential framework. We then show that its existence is equivalent to that of an incremental Lyapunov-Krasovskii functional and guarantees uniform incremental exponential stability. Next, we extend the concept of Lyapunov-Razumikhin functions within the differential framework, whose existence also ensures uniform incremental exponential stability. As an application of our results, we formulate stabilizing feedback control design for nonlinear time-delay systems with single delays in terms of linear matrix inequalities.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：时滞广泛存在于通信网络、经济系统、生物过程等实际系统中，会引发不稳定或复杂动力学行为。现有的收缩分析（contraction analysis）主要针对无时滞系统（即常微分方程描述的延迟自由系统），能够刻画任意两条轨迹之间的增量稳定性，但尚未系统化地推广到非线性时滞系统（由泛函微分方程描述）。
- **研究动机**：已有的一些工作仅针对特定结构（如通过时滞通信互联的延迟自由系统、合作/单调系统、离散时间随机系统等），或仅处理常数/时变时滞，且未建立微分 Lyapunov 方法与增量 Lyapunov 方法之间的等价关系。本文旨在将收缩分析推广到一般非线性时滞系统，建立统一的理论框架，并给出可数值求解的控制器设计条件。
- **整体含义**：本文为时滞系统的增量指数稳定性提供了基于微分 Lyapunov–Krasovskii 泛函和微分 Lyapunov–Razumikhin 函数的新判据，证明了两类泛函的存在等价性，并进一步利用线性矩阵不等式（LMI）设计了单时滞非线性系统的镇定控制器。该工作填补了收缩分析在时滞系统领域的理论空白，并为后续控制器综合提供了计算可行的途径。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（用文字说明即可）

- **核心思想**：将收缩分析中的“微分 Lyapunov 函数”思想扩展到泛函微分方程描述的时滞系统，利用 Fréchet 导数定义沿轨道的变分系统，然后引入微分 Lyapunov–Krasovskii 泛函和微分 Lyapunov–Razumikhin 函数，并证明它们与增量 Lyapunov–Krasovskii 泛函的等价性，从而建立统一框架。
- **关键技术细节**：
  1. **系统模型**：考虑泛函微分方程 \(\dot{x}(t) = f(t, x_t)\)，其中 \(x_t(\theta)=x(t+\theta),\ \theta\in[-r,0]\)。
  2. **变分系统**：沿解 \(\chi(t_0,\phi)(t)\) 定义变分系统 \(\dot{\delta x}(t) = D_{\chi_t} f(t,\chi_t(t_0,\phi))\,\delta x_t\)，其中 \(D_{\chi_t}f\) 是 Fréchet 导数。
  3. **微分 Lyapunov–Krasovskii 泛函**：定义 \(V_D(t,\phi,\delta\phi)\)，满足正定性（下界只依赖 \(\delta\phi(0)\)，上界依赖 \(\|\delta\phi\|_C\)）以及沿变分轨迹的积分下降不等式。通过路径积分构造等价增量 Lyapunov–Krasovskii 泛函，反之通过极限构造反向转换。
  4. **微分 Lyapunov–Razumikhin 函数**：定义 \(\bar{V}_D(t,x,\delta x)\)，满足正定性及 Razumikhin 条件：若 \(\bar{V}_D(t+\theta,\phi(\theta),\delta\phi(\theta)) \le \rho \bar{V}_D(t,\phi(0),\delta\phi(0))\)，则其 Dini 导数负定。
  5. **控制器设计**：针对系统 \(\dot{x}(t)=f(x(t),x(t-r))+Bu(t)\)，利用凸松弛将非线性项包含在矩阵多胞形中，选取候选微分 Lyapunov–Krasovskii 泛函（包含状态项、延时积分项和导数项积分），通过 LMI 求解反馈增益 \(F_1,F_2\)，并给出 LMI 可凸化的变换技巧（取 \(P_3=\varepsilon P_2\) 等）。
- **算法流程**：给定非线性时滞系统 → 计算雅可比矩阵关于状态和时滞的凸包表示 → 将 LMI 条件转化为标准线性矩阵不等式 → 求解获得控制器增益。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **实验场景**：论文仅在第 V 节提供了一个数值示例，采用一个 2 维非线性时滞系统：
  \[
  \begin{cases}
  \dot{x}_1 = -x_1(t) + \tanh x_2(t-5) + a\sin 2t \\
  \dot{x}_2 = -\sin x_1(t) - x_2(t-5) + u(t)
  \end{cases}
  \]
  时滞 \(r=5\)，参数 \(a\) 分别取 0 和 1（后者系统无平衡点）。
- **Benchmark**：没有与已有方法进行显式对比。示例主要证明所提出的 LMI 设计可以成功镇定系统并能处理不存在平衡点的情形。
- **对比方法**：无。作者通过展示不同初始条件的轨迹汇聚来验证增量指数稳定性，并未设置其他控制器设计方法作为对照。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点

论文未提及任何算力、GPU 型号、训练时长等信息。由于仅涉及求解线性矩阵不等式（LMI）和一个数值仿真示例，计算量很小，不需要高性能计算资源。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

- **实验数量**：只进行了一组数值示例，包含两种情形（\(a=0\) 和 \(a=1\)），每种情形随机生成 30 条初始轨迹。没有消融实验、参数敏感性分析、不同时滞长度对比或与其他方法的公平比较。
- **充分性**：作为一篇理论性论文，实验主要用于说明理论的可行性，但不充分。缺少对不同时滞值、不同系统结构、高维系统、抗干扰能力等的验证；没有对照基准方法，无法评估所提设计相对已有工作的优劣；没有讨论 LMI 可行性的保守性。因此实验的充分性、客观性和公平性均显不足。

### 6. 论文的主要结论与发现

- **理论贡献**：
  - 提出了针对一般非线性时滞系统的微分 Lyapunov–Krasovskii 泛函和微分 Lyapunov–Razumikhin 函数，并证明它们的存在性等价于系统的一致增量指数稳定性（对于 Lyapunov–Krasovskii 类，存在等价性；对于 Razumikhin 类，仅给出充分条件）。
  - 建立了微分与增量 Lyapunov–Krasovskii 泛函之间的相互转换关系：通过路径积分和极限构造，证明两者等价。
- **应用贡献**：
  - 针对单时滞非线性系统，给出了基于 LMI 的状态反馈镇定控制器设计方法，该方法计算可解，且对时滞长度具有鲁棒性（若对某 \(r_{\max}\) 可行，则对所有 \(0<r\le r_{\max}\) 可行）。
- **示例验证**：数值例子表明，所设计的控制器能使不同初始条件的轨迹收敛到一起，即使系统不存在平衡点（\(a=1\)）也仍能实现增量指数稳定性。

### 7. 优点：方法或实验设计上有哪些亮点

- **理论深度**：将收缩分析从常微分方程推广到泛函微分方程，涉及 Fréchet 导数、路径积分在函数空间中的运用，技术推导严谨，为解决时滞系统增量稳定性提供了系统化工具。
- **等价性证明**：首次在时滞系统框架下证明了微分与增量 Lyapunov–Krasovskii 泛函的等价性，填补了该领域的空白。
- **实用性**：将非线性镇定问题转化为 LMI，使得控制器设计可以借助现有凸优化工具求解，降低了应用门槛。
- **鲁棒性**：设计出的控制器对时滞长度在区间内变化保持有效，具有一定鲁棒性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖不足**：仅有一个低维示例，缺少对高维系统、多个时滞、时变时滞、不确定性等的验证；没有与现有方法（如非线性模型预测控制、基于 Lyapunov–Krasovskii 的线性化设计）进行对比，无法评估性能优劣。
- **理论局限**：
  - 增量指数稳定的充分条件依赖于全局 Lipschitz 假设（在等价性证明中），该假设对许多实际系统可能过强。
  - 对于 Lyapunov–Razumikhin 函数，仅建立了充分性，未得到逆定理，且与微分 Lyapunov–Krasovskii 泛函的关系未被探讨。
  - LMI 设计方法假设非线性项可以凸包表示，且要求状态空间 \(\Omega\) 为凸集并满足解的整体有界性，这在应用中可能难以保证。
- **偏差风险**：数值示例中通过调节参数 \(\varepsilon\) 来使 LMI 可行，文中未讨论如何系统选择 \(\varepsilon\)；示例中 \(\varepsilon=3.07\times10^{-3}\)，该值很小，可能暗示矩阵条件数很高，实际求解时可能数值敏感。
- **应用限制**：所提方法限于单时滞系统，且反馈控制律要求同时使用当前状态和时滞状态，这意味着需要时滞状态可测或状态估计，在通信等系统中可能不现实。

（完）
