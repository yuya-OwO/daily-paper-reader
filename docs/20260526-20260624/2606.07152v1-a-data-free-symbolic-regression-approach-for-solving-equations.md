---
title: A Data-Free Symbolic Regression Approach for Solving Equations
title_zh: 一种求解方程的无数据符号回归方法
authors: "Sergei Garmaev, Vinay Sharma, Olga Fink"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07152v1"
tags: ["query:sr"]
score: 9.0
evidence: 提出了一种无需数据的符号回归方法来求解方程
tldr: 现有符号回归方法需从过程获取训练数据，但许多科学方程无法解析求解。本文提出符号方程求解器SES，仅利用方程与初边条件构造可微目标函数，将求解转化为优化问题。SES在代数方程、常微分方程和偏微分方程上恢复出与解析解匹配的紧凑符号表达式。该方法无需数据即可从方程直接学习显式符号解，便于后续数学分析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07152v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1553, \"height\": 1287, \"label\": \"Figure\"}]"
motivation: 现有符号回归需依赖输入输出数据，而非仅从控制方程出发，限制了无数据场景下的解析求解。
method: SES将方程求解转化为优化问题，利用可微符号模型，从方程和初边条件构建目标函数，无需配对数据。
result: SES在代数方程、含超越项方程、ODE和PDE等案例中恢复了与解析解一致的紧凑符号表达式。
conclusion: SES无需训练数据，直接从方程学习显式符号解，拓展了符号回归在方程求解中的应用。
---

## 摘要
许多出现在科学中的方程目前无法通过现有的解析技术求解，因此采用数值方法求解，而不产生显式的符号表达式。现有的符号回归方法可以恢复符号表达式，但需要从底层过程中获取训练数据，而不仅仅是控制方程。我们提出了符号方程求解器（SES），这是一个将方程求解转化为关于可微符号模型的优化问题的框架。SES从方程以及初始条件或边界条件中构造其目标函数，从而消除了对配对输入输出数据的需求。学习到的模型以显式符号形式表达，便于进一步分析。我们在代表性的代数和微分方程上评估了SES，包括一个代数方程组、一个含有超越项的方程、一个常微分方程以及具有不同初始或边界条件的偏微分方程。在这些设置中，SES恢复了与相应解析解匹配的紧凑符号表达式。

## Abstract
Many equations arising in science currently cannot be solved by available analytical techniques and are therefore solved numerically, without yielding explicit symbolic expressions. Existing symbolic regression approaches can recover symbolic expressions, but require training data obtained from the underlying process, rather than the governing equation alone. We propose the Symbolic Equation Solver (SES), a framework that formulates equation solving as an optimization problem over differentiable symbolic models. SES constructs its objective from the equation together with initial or boundary conditions, eliminating the need for paired input-output data. The learned model is expressed in explicit symbolic form, enabling further analysis. We evaluate SES on representative algebraic and differential equations, including a system of algebraic equations, an equation with transcendental terms, an ordinary differential equation, and partial differential equations with different initial or boundary conditions. Across these settings, SES recovers compact symbolic expressions that match the corresponding analytical solutions.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义
- **研究动机**：许多科学方程无法解析求解，只能依赖数值方法得到近似数值解，缺乏显式符号表达式。现有的符号回归方法（如EQL、PySR）虽然能恢复符号形式，但**必须依赖输入-输出配对数据**，而方程求解问题通常只给出控制方程和初边条件，没有监督数据。
- **关键缺口**：同时满足三个属性——直接基于控制方程、无需监督数据、输出显式符号表达式——的方法尚不存在。
- **论文定位**：提出**SES（Symbolic Equation Solver）**，将方程求解转化为在可微符号模型上的残差优化问题，从方程和初边条件直接学习显式符号解，填补了方程驱动与符号恢复之间的空白。

## 2. 方法论
- **核心思想**：使用可微符号网络（基于EQL架构）表示候选解，用自动微分计算导数，构造由**方程残差**和**初边条件残差**组成的目标函数，在随机采样配点处最小化，最后从训练后的稀疏网络中提取符号表达式。
- **关键技术细节**：
  - **符号模型**：两层全连接网络，每层包含预先定义的符号算子集 `{id, const, (·)², exp(·), tanh(·), ×}`，每个算子每层重复两次。最大树深度为2。
  - **目标函数**：$$L(\theta) = \frac{1}{N_F}\sum_{i=1}^{N_F}\sum_{k=1}^K F_k^2 + \frac{1}{N_B}\sum_{i=1}^{N_B}\sum_{j=1}^J B_j^2$$ 其中 $F_k$ 为方程残差，$B_j$ 为边界/初值残差，配点每轮独立采样256个。
  - **优化策略**：三阶段训练——① 仅残差预训练50k epoch；② 加 ℓ1 惩罚并周期性剪枝（直到保留5个活跃权值，泊松方程保留更多）200k epoch；③ 移除惩罚微调20k epoch。使用Adam优化器。最终权值四舍五入到5位小数。
  - **导数计算**：通过自动微分获得符号模型的导数，用于微分方程残差。
  - **提取符号表达式**：遍历训练后的计算图，将权值与算子组合成显式符号形式。

## 3. 实验设计
- **测试场景**：5个代表性方程，覆盖代数、超越、ODE、PDE。
  1. **线性方程组** $\{2x+3y=7, x-y=1\}$，解析解 $x=2, y=1$。
  2. **超越方程** $e^x + x^3 = e + 1$，唯一实解 $x=1$。
  3. **非线性ODE** $dy/dt = 1 - y^2,\ y(0)=0$，解析解 $y=\tanh t$。
  4. **线性输运方程** $\partial u/\partial t + \partial u/\partial x = 0,\ u(x,0)=x^2$，解析解 $u=(x-t)^2$。
  5. **二维泊松方程** $\partial^2 u/\partial x^2 + \partial^2 u/\partial y^2 = 4 + 2x$，两组混合边界条件（解析解分别为 $x^2+y^2+xy^2$ 和 $2x^2+xy^2$）。
- **Benchmark与对比**：**无**。论文未与任何现有方法（如PINNs、传统符号回归）进行定量对比，因为SES是首个在无数据设置下从方程恢复符号解的方法。定性上与解析解对照。
- **数据来源**：无需数据集，仅通过方程和初边条件产生残差信号。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量、训练总时长或硬件配置。仅提到使用Adam优化器，配点每轮256个，训练总epoch数约270k（50k+200k+20k）。因此无法评估计算成本。

## 5. 实验数量与充分性
- **数量**：共5组实验，每个方程一个案例。
- **充分性分析**：
  - **正面**：覆盖了代数、超越、ODE、PDE等基本方程类型，展示了框架的通用性。
  - **不足**：
    - 所有测试方程都具有已知的、相对简单的解析解，且均能被符号算子库表示；未测试更复杂、非线性更强或高维的方程（如混沌系统、Navier-Stokes方程）。
    - 未进行**消融实验**（如不同算子库、不同深度、不同剪枝策略的影响）。
    - 未与数值方法（如PINNs）比较精度、收敛速度或计算效率。
    - 未测试噪声、稀疏配点或更复杂的边界条件。
    - 仅给出了符号表达式是否匹配，未量化残差大小或误差。
  - **客观性**：结果展示较为诚实（如输运方程恢复的系数为近似值0.95184和1.025），但未讨论失败情况或收敛难度。

## 6. 主要结论与发现
- SES能够在**无监督数据**的条件下，仅从方程和初边条件优化得到与解析解匹配的显式符号表达式。
- 对于代数方程（包括超越项），SES能恢复精确常数解；对于微分方程，能恢复正确的非线性函数形式（如 $\tanh t$、$(x-t)^2$、多项式）。
- 框架统一适用于标量和系统（多个未知量联合优化）。
- 学习到的符号表达式紧凑、可解释，便于进一步数学分析。

## 7. 优点
- **无数据需求**：解决了传统符号回归必须依赖输入-输出配对数据的根本局限。
- **输出显式符号形式**：相比PINNs等神经网络数值解，SES直接得到可分析的符号表达式，有利于揭示函数依赖、对称性和定性行为。
- **统一框架**：同一个架构和优化流程适用于代数方程、ODE和PDE，无需针对不同类型修改。
- **可扩展性**：可自然推广到多未知量耦合系统（如线性方程组和泊松方程的多变量输出）。

## 8. 不足与局限
- **表达能力受限**：当前最大深度为2，算子库有限（仅6种算子），无法处理需要高阶导数、更复杂函数（如三角函数、对数、分数幂等）或深层嵌套的表达。
- **实验覆盖狭窄**：仅测试已知解析解的低阶简单方程，未验证在无解析解或混沌系统中的有效性；未做消融实验或量化对比。
- **收敛性与超参数敏感**：优化依赖三阶段训练、剪枝阈值和算子设计，未讨论对不同方程的调参难度或失败案例。
- **未考虑效率**：缺乏与数值方法的速度或精度对比；未报告完整训练时间或计算资源消耗。
- **理论保证不足**：无收敛性分析或误差界证明；提取的符号表达式仅近似，且需人工验证其是否真正满足方程。

（完）
