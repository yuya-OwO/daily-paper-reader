---
title: "Learning Neural Maximal Lyapunov Functions on $\\mathsf{SO}(n)$"
title_zh: "在$\\mathsf{SO}(n)$上学习神经最大李雅普诺夫函数"
authors: "Adeel Akhtar, Matthieu Barreau"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.19669v1"
tags: ["query:lyapunov-ai"]
score: 9.0
evidence: 在SO(n)上学习最大李雅普诺夫函数的神经架构
tldr: 针对特殊正交群SO(n)上动力系统的稳定性分析，现有李雅普诺夫方法难以直接应用。本文提出基于对数映射的神经李雅普诺夫架构，具备近似能力，并通过Zubov型刻画最大吸引域。关键技术推导了对数映射导数的显式数值公式，采用两阶段算法平衡效率与精度。在低维非线性系统上的实验验证了方法的有效性，为李群上的稳定性学习提供了新框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19669v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 493, \"height\": 370, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19669v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 743, \"height\": 1273, \"label\": \"Figure\"}]"
motivation: 欧氏空间的李雅普诺夫方法无法直接用于曲率非零的李群，缺乏通用稳定性学习框架。
method: 基于对数映射设计神经李雅普诺夫函数，推导其导数显式公式，结合Zubov方程构建最大吸引域学习问题。
result: 在SO(n)低维系统上成功学习到最大李雅普诺夫函数，吸引域估计准确且训练高效。
conclusion: 首次为SO(n)上的稳定性保证提供可学习的神经李雅普诺夫方法，可推广至其他李群。
---

## 摘要
为李群上的动力系统建立稳定性保证是一项基本挑战，因为为欧几里得空间开发的经典李雅普诺夫方法无法直接应用于弯曲几何。本文提出了一个框架，用于学习在特殊正交群$\mathsf{SO}(n)$上演化的系统的最大李雅普诺夫函数。在理论上，我们引入了一种基于对数映射的神经李雅普诺夫架构，并证明了其逼近能力，通过最大吸引域的Zubov型特征来表述学习问题。一个关键的技术贡献是推导了对数映射导数的显式、数值上易处理的公式，使得通过一种平衡计算效率和准确性的两阶段算法进行训练成为可能。在实证中，我们在一个低维非线性系统上验证了该方法。

## Abstract
Establishing stability guarantees for dynamical systems on Lie groups is a fundamental challenge, as classical Lyapunov methods developed for Euclidean spaces do not directly transfer to curved geometries. In this paper, we propose a framework for learning maximal Lyapunov functions for systems evolving on the special orthogonal group $\mathsf{SO}(n)$. Theoretically, we introduce a neural Lyapunov architecture based on the logarithmic map with proven approximation capabilities, and we formulate the learning problem via a Zubov-type characterization of the maximal region of attraction. A key technical contribution is the derivation of explicit, numerically tractable formulas for the derivative of the logarithmic map, enabling training through a two-phase algorithm that balances computational efficiency and accuracy. Empirically, we validate the approach on a low-dimensional nonlinear system.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：为演化在特殊正交群 $\mathsf{SO}(n)$ 上的动力系统建立稳定性保证，尤其是估计最大吸引域（Region of Attraction, ROA）。由于 $\mathsf{SO}(n)$ 是弯曲流形，经典的欧几里得空间李雅普诺夫方法无法直接应用。
- **整体意义**：首次提出针对 $\mathsf{SO}(n)$ 的神经李雅普诺夫函数学习框架，将最大吸引域的 Zubov 型特征与对数映射结合，为李群上的非线性稳定性学习提供了理论可行性和计算途径。

### 2. 论文提出的方法论
- **核心思想**：
  - 利用对数映射 $\text{Log}$ 将 $\mathsf{SO}(n)_{\text{reg}}$ 局部微分同胚于欧几里得空间 $\mathbb{R}^{m_n}$（$m_n = n(n-1)/2$），从而将李群上的李雅普诺夫函数构造转化为欧几里得空间上的函数逼近问题。
  - 结合 Zubov 方程刻画最大吸引域，将学习目标定义为使残差 $r(R,x)=0$ 的优化问题。
- **关键技术细节**：
  - **神经李雅普诺夫架构**：$V_{\theta_1,\alpha}(R,x) = \|\psi_{\theta_1}(\gamma(R),x) - \psi_{\theta_1}(0,0)\|^2 + \alpha\|(R,x)\|^2_K$，其中 $\gamma(R)=\text{Log}(R)^\vee$，$\psi_{\theta_1}$ 是前馈神经网络。该架构保证正定性且具有通用逼近能力（命题1、2）。
  - **Zubov 型损失函数**：基于等式 $r(R,x) = \langle (R^\top \nabla_R V)^\vee, x \rangle + \langle \nabla_x V, f \rangle + \phi(1-V) = 0$，其中 $\phi$ 正定。通过最小化解的平方误差来学习 $V$ 和 $\phi$。
  - **对数映射导数公式**：推导出左平凡化的梯度 $J_\ell^\gamma(R)$，其核心是 $\left(d^\ell \text{Exp}_X\right)^{-1}$ 的计算。提供两种数值方法：
    - **级数近似**：利用 Bernoulli 级数截断，复杂度 $O(M n^3)$，用于早期训练。
    - **精确计算**：通过块矩阵指数构造 Jacobian 并求解线性系统，复杂度 $O(n^5)$，用于最终阶段。
  - **两阶段训练算法**：早期使用级数近似加速，后期切换精确计算保证准确性；结合频繁重采样避免过拟合。

### 3. 实验设计
- **数据集/场景**：仅使用一个低维测试系统：$n=2$（$m_n=1$），动力学 $\dot{R}=R\hat{x}$，$\dot{x}=-x+x^2-2(R-R^\top)^\vee$，状态空间为 $(\omega,x)\in [-\pi/2,\pi/2]\times[-2,2]$。
- **Benchmark**：未提供公开 benchmark 或对比方法。论文自述，仅取二次项（$\psi_\theta\equiv0$）将导致学习无解，以此间接凸显神经方法的必要性。
- **对比方法**：无显式对比其他 Lyapunov 学习或经典方法（如 SOS、PINN）。

### 4. 资源与算力
- **算力信息**：文中未明确提及使用的 GPU 型号、数量、训练时长或总计算量。仅说明代码基于 Python + TensorFlow，并开源在 GitHub。

### 5. 实验数量与充分性
- **实验数量**：只有一个场景（$n=2$）、一个系统配置，未展示消融实验、不同超参数影响或更高维的 $\mathsf{SO}(3)$ 等案例。
- **充分性与公平性**：实验规模极小，仅用于概念验证；未与任何已有方法定量比较，无法评估在相同问题上的相对性能；也未报告统计变异或多次运行结果。因此，充分性和公平性有限。

### 6. 论文的主要结论与发现
- 成功为 $\mathsf{SO}(2)$ 上的非线性系统学到了一个近似最大李雅普诺夫函数，其 Fréchet 导数负定，吸引域估计覆盖了理论最大吸引域的 64.3%。
- 纯二次候选函数无法解决该问题，而本文的神经构造可行，证明了方法的实际效果。
- 对数映射导数的显式公式使训练可行，两阶段策略在效率与精度之间取得了平衡。

### 7. 优点
- **理论深度**：证明了所提架构的通用逼近能力（命题1、2）及 Zubov 方程在李群上的适配性（命题3）。
- **技术原创**：推导了对数映射导数的显式、数值易处理的公式（级数与精确两种），解决了自动微分库无法直接计算梯度的问题。
- **架构合理**：通过 $\gamma(R)$ 将 $\mathsf{SO}(n)$ 降维至固有维度，同时保持几何结构；通过范数平方项保证光滑正定性。
- **算法设计**：两阶段训练策略平衡了计算成本与数值精度，具有实用价值。

### 8. 不足与局限
- **实验覆盖严重不足**：仅验证了 $n=2$ 的情形，未在 $\mathsf{SO}(3)$ 或更高维上测试，实际应用推广性未知。
- **缺乏对比与消融**：未与经典的 Lyapunov 求解方法（如 SOS、多项式方法）或其它神经网络方法（如 PINN-Lyapunov）做定量对比；也未分析不同 $\alpha$、网络大小、重采样频率等超参数的影响。
- **地域限制**：架构仅定义在 $\mathsf{SO}(n)_{\text{reg}}$（排除旋转角为 $\pi$ 的点），覆盖不了整个 $\mathsf{SO}(n)$，对于旋转 $\pi$ 附近的状态可能失效。
- **偏倚风险**：实验选择一个自构造的系统，且 f 中的项 $2(R-R^\top)^\vee$ 直接与 $\gamma$ 相关，可能偏向于方法，泛化性存疑。
- **计算资源未披露**：无法评估方法的训练成本，不利于复现和比较。
- **收敛性保证不足**：未提供学习过程收敛到真实 Zubov 解的严格理论保证。

（完）
