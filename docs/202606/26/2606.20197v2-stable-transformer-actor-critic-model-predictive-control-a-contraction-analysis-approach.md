---
title: "Stable Transformer-Actor-Critic Model Predictive Control: A Contraction Analysis Approach"
title_zh: 稳定Transformer-演员-评论家模型预测控制：一种收缩分析方法
authors: "Antonio Marino, Valerio Modugno, Marco Cognetti"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20197v2"
tags: ["query:lyapunov-ai"]
score: 8.0
evidence: 证明了Transformer网络满足增量输入-状态稳定性，可应用于李雅普诺夫函数发现
tldr: 现有Actor-Critic MPC在处理复杂控制问题时难以保证闭环稳定性。本文提出Transformer-Actor-Critic MPC架构，首先证明Transformer网络满足全局增量输入状态稳定性(δISS)，然后利用Riemannian收缩理论分析物理系统与神经网络的互连动态，最后将理论界作为训练正则化项，得到可认证鲁棒策略。在非线性3D无人机任务上验证其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20197v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 850, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20197v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 894, \"height\": 919, \"label\": \"Figure\"}]"
motivation: Actor-Critic MPC缺乏闭环稳定性形式保证，难以用于安全关键场景。
method: 证明Transformer满足δISS，结合Riemannian收缩理论分析互连动态，并将理论界作为训练正则化。
result: 在非线性3D无人机目标到达和避障任务中验证了策略的鲁棒性。
conclusion: 提出可认证鲁棒的Transformer-Actor-Critic MPC，为序列控制提供稳定性保证。
---

## 摘要
演员-评论家模型预测控制（MPC）有效地解决了复杂的非凸控制问题，但保证序列学习模型在这些管线中的闭环稳定性仍然具有挑战性。本文提出了一种具有形式鲁棒性保证的新型Transformer-演员-评论家MPC架构。首先，我们证明了Transformer网络能够满足全局增量输入-状态稳定性（δISS）。然后，我们利用黎曼收缩理论分析物理对象与预测神经网络之间的互联动态。最后，我们将这些理论界作为训练正则化器整合到策略中，从而得到可证明鲁棒的策略。该框架在一个执行目标到达和避障 maneuvers 的非线性3D无人机模型上得到了验证。

## Abstract
Actor-Critic Model Predictive Control (MPC) effectively addresses complex, non-convex control problems, but guaranteeing the closed-loop stability of sequence-based learning models within these pipelines remains challenging. This paper introduces a novel Transformer-Actor-Critic MPC architecture with formal robustness guarantees. First, we prove that Transformer networks can satisfy global incremental Input-to-State Stability ($δ$ISS). We then leverage Riemannian contraction theory to analyze the interconnected dynamics between the physical plant and the predictive neural network. Finally, we integrate these theoretical bounds as a training regularizer to yield a certifiably robust policy. The framework is validated on a nonlinear 3D drone model executing target-reaching and obstacle-avoidance maneuvers.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Stable Transformer-Actor-Critic Model Predictive Control: A Contraction Analysis Approach》的详细中文总结。

## 论文核心总结

### 1. 论文的核心问题与整体含义

- **研究动机**：Actor-Critic 模型预测控制（MPC）方法能有效处理复杂、非凸的控制问题，但其核心缺陷在于难以提供闭环稳定性的形式化保证。特别是当使用序列模型（如 Transformer）时，由于缺乏理论约束，策略的鲁棒性无法被认证，这在安全关键的应用场景（如无人机控制）中是致命的。
- **核心问题**：如何为使用序列模型（Transformer）的 Actor-Critic MPC 框架提供严格的、可证明的闭环稳定性保证。
- **整体含义**：本文旨在弥合基于学习的控制与形式化稳定性理论之间的鸿沟，提出一种理论上可认证鲁棒的混合控制架构，使得高性能的序列模型能够安全地应用于现实世界的控制任务。

### 2. 论文提出的方法论

#### 核心思想
本文提出一种 **Stable Transformer-Actor-Critic MPC (STAC-MPC)** 架构，其核心思想是：
1.  **约束 Transformer**：从理论上证明 Transformer 网络可以满足增量输入-状态稳定性 (δISS)。
2.  **耦合分析**：利用黎曼收缩理论分析物理系统与预测神经网络（Transformer）互联后的整体动态。
3.  **正则化训练**：将推导出的互联系统收缩条件（小增益条件）作为训练正则化项，强制网络学习一个**可认证鲁棒**的策略。

#### 关键技术细节
-   **Transformer 的 δISS 证明**：
    -   论文将 Transformer 层视为一个离散动态系统。
    -   通过严谨的数学推导，找到了一个关于网络权重和参数的充分条件（定理1：\( A_\delta < 1 \)），保证该系统的增量状态偏差 \( \|\delta z_k\|_\infty \) 是**有界**的，且由输入偏差 \( \|\delta p_k\|_\infty \) 决定，从而满足 δISS 定义。
    -   核心约束项 \( A_\delta \) 包含了 Transformer 各组件（注意力机制、MLP、层归一化）的 Lipschitz 常数。

-   **互联系统的小增益条件**：
    -   将物理系统和 Transformer 网络视为两个互联的子系统，并构建了它们的误差动态方程。
    -   物理系统（MPC 控制的系统）被证明在黎曼度量下以速率 \( \rho_{mpc} \) 收缩。
    -   Transformer 网络（预测参数 \( Y \)）以速率 \( A_\delta \) 收缩。
    -   两个系统通过耦合增益 \( \gamma_z \) 和 \( \gamma_x \) 相互影响。
    -   最终，推导出整个互联系统收敛的充分必要条件（定理2）：\( (1-\rho_{mpc})(1-A_\delta) - \gamma_z\gamma_x > 0 \)。这个**小增益条件**确保了整个闭环系统的统一收缩（稳定性）。

-   **算法流程**：
    1.  **输入**：历史状态序列 \( x_{k-L:k} \) 和参考序列 \( x_{d,k-L:k} \)。
    2.  **特征提取**：通过初始 MLP 将输入映射为 Transformer 的输入序列。
    3.  **Transformer 预测**：Transformer 处理序列信息，输出 \( z_k \)。
    4.  **参数投影**：通过最终 MLP 将 \( z_k \) 映射为 MPC 的代价矩阵 \( Q \) 和 \( P \)。
    5.  **可微分 MPC**：使用可微分优化器求解 MPC 问题，得到最优控制序列 \( \omega^* \)。
    6.  **损失计算与正则化**：除正常任务损失外，额外施加一个正则化损失项，强制网络满足推导出的**小增益条件**。
    7.  **反向传播**：通过 KKT 条件的隐函数定理 (IFT) 将梯度从 MPC 求解器传递回网络，进行端到端训练。

### 3. 实验设计

-   **场景与任务**：非线性 3D 无人机（四旋翼）模型，执行两个任务：
    -   **目标到达**：从初始位置飞向目标位置，**无**障碍物。
    -   **避障**：飞向目标位置，路径中**存在**障碍物。
-   **Benchmark / 对比方法**：
    -   **本文方法 (STAC-MPC)**：使用 δISS 约束和收缩理论正则化的 Stable Transformer-Actor-Critic MPC。
    -   **Baseline (Vanilla)**：一个未施加任何稳定性正则化的普通 Actor-Critic MPC [7]。
-   **数据**：未明确提及使用特定数据集。实验基于仿真环境，每次随机初始化状态和目标。鲁棒性测试通过施加高幅度**均匀分布的观测噪声** (\( n_k \sim U(-0.4, 0.4) \)) 和**过程扰动** (\( d_k \sim U(-0.5, 0.5) \)) 来模拟。

### 4. 资源与算力

-   **文中未明确说明**使用的算力细节（如 GPU 型号、数量、训练时长）。

### 5. 实验数量与充分性

-   **实验数量**：
    -   针对每个场景（目标到达和避障），执行了 **10 次随机化的验证**。
    -   报告了跟踪误差的**均值和标准差**。
-   **充分性评估**：
    -   **优点**：实验设计清晰地对比了有无稳定性正则化的差异，且在高干扰下进行测试，验证了方法的**核心价值**。避障中统计了碰撞率，提供了量化指标。
    -   **不足**：
        -   **实验规模较小**：仅有10次随机验证，统计显著性可能不足。
        -   **场景单一**：仅在单一类型的四旋翼模型上进行验证，未扩展至其他机器人平台。
        -   **缺乏消融实验**：没有对正则化项的各个组成部分（如 \( A_\delta \) 约束、小增益条件）进行单独的消融研究。
        -   **没有与其他鲁棒控制方法对比**：仅对比了无正则化的 baseline。
    -   **总体评价**：实验初步验证了理论的有效性，但其**充分性和客观性**不足以支撑一个广泛的结论，更像是概念验证。

### 6. 论文的主要结论与发现

-   **理论贡献**：**成功证明了** Transformer 网络可以满足全局 δISS，并**推导出**一个由网络权重决定的、可操作的条件。
-   **方法验证**：所提出的将互联系统收缩条件作为训练正则化项的方法是有效的。
-   **鲁棒性提升**：在强干扰下，**稳定的网络 (STAC-MPC) 能保持飞行稳定**，成功到达目标并避开障碍物（0% 碰撞率），而**未正则化的网络 (Vanilla) 则出现灾难性发散和坠毁**。
-   **轨迹一致性**：稳定网络的轨迹在相同初始条件下高度一致，这从经验上验证了其 δISS 性质。
-   **性能权衡**：严格的全局稳定性约束**轻微限制了网络的灵活性**，导致在瞬时姿态跟踪的锐利度上略逊于不稳定的 Vanilla 模型（在发散前）。

### 7. 优点

-   **理论创新性强**：首次将 Transformer 的 δISS 证明与收缩理论结合，为序列模型在控制中的稳定性提供了形式化工具。
-   **方法逻辑严谨**：从独立模块的稳定性证明，到互联系统的耦合分析，再到训练正则化，形成了一个完整的理论闭环。
-   **理论与实践结合紧密**：理论推导出的可计算条件（如 \( A_\delta \) 和小增益条件）能直接作为网络训练的约束，实现了可证明的鲁棒性。
-   **问题定义清晰**：精准地指出了现有 Actor-Critic MPC 缺乏稳定性保证这一核心痛点，并提出了解决方案。

### 8. 不足与局限

-   **实验验证薄弱**：实验规模小、场景单一，且缺乏与同类鲁棒控制方法（如基于 CBF 或鲁棒 MPC）的对比，削弱了实证的说服力。
-   **潜在应用限制**：
    -   假设系统状态和控制量有界。
    -   推导出的 \( A_\delta \) 条件可能由于层归一化中 \( \sigma_{min} \) 很小而变得保守，虽然作者建议用 \( \epsilon \) 缓解，但本质仍是**全局、保守的界**，可能导致策略性能下降。
    -   方法依赖**可微分的 MPC 求解器**，这在一些系统中可能不易实现。
-   **计算与工程复杂性**：Transformer 和可微分 MPC 的组合可能导致计算开销显著增高，文中未提及推理速度。
-   **偏差风险**：文中的“tighter nominal attitude tracking”（更紧的名义姿态跟踪）可能表明，在某些工况下，过度追求稳定性可能损害了任务完成度（如快速机动），这需要根据不同应用场景进行权衡。

（完）
