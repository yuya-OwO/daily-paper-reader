---
title: "Symbolic Discovery of Iterative Algorithms: A Continuous Latent Space Bayesian Optimization Framework"
title_zh: 迭代算法的符号发现：一种连续潜在空间贝叶斯优化框架
authors: "Tongjia Liu, Ilias Mitrai"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01552v1"
tags: ["query:sr"]
score: 8.0
evidence: 符号形式的迭代算法发现
tldr: 本文针对迭代优化算法的自动发现难题，将其转化为离散优化问题。提出一种基于连续潜在空间贝叶斯优化的框架，先利用变分自编码器学习更新函数的连续表示，再通过贝叶斯优化搜索新算法。在案例中成功发现符号形式的新更新函数，无需预设函数形式。该方法计算效率优于传统数学规划方法，实现了算法发现的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1782, \"height\": 649, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1891, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 913, \"height\": 544, \"label\": \"Figure\"}]"
motivation: 自动发现迭代优化算法，无需人工设计，提高算法创新效率。
method: 使用变分自编码器将离散更新函数空间映射到连续潜在空间，再通过贝叶斯优化搜索新函数。
result: 在案例中成功发现符号形式的新更新函数，计算时间低于数学规划方法。
conclusion: 提出一种高效自动发现迭代算法的新框架，无需假设函数形式，拓展算法设计空间。
---

## 摘要
在本文中，我们考虑迭代优化算法的自动发现。我们将算法发现任务形式化为一个离散优化问题，并使用潜在空间贝叶斯优化搜索新的更新函数。所提出的框架首先利用变分自编码器学习更新函数离散空间的连续表示，从而将算法发现任务从离散搜索问题转变为连续搜索问题。随后，利用该连续表示通过贝叶斯优化搜索新算法。在两个案例研究中的应用表明，所提出的方法可以在不对更新函数形式做任何假设的情况下，以符号形式发现新的更新函数。此外，发现新更新函数所需的计算时间少于现有的基于数学规划的方法。

## Abstract
In this paper, we consider the automated discovery of iterative optimization algorithms. We formulate the algorithm discovery task as a discrete optimization problem and search for new update functions using latent space Bayesian Optimization. The proposed framework first learns a continuous representation of the discrete space of update functions using variational autoencoders, transforming the algorithm discovery task from a discrete to a continuous search problem. The continuous representation is subsequently used to search for new algorithms using Bayesian optimization. Application to two case studies shows that the proposed approach can discover new update functions in symbolic form without any assumptions on the functional form of the update function. Moreover, the computational time required to discover the new update functions is lower than existing mathematical programming-based approaches.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：迭代优化算法（如梯度下降）通常依赖人工设计更新函数，耗时且易受领域知识限制。现有自动发现方法面临可解释性与计算可扩展性的矛盾：基于神经网络的方法产生黑盒函数，基于进化算法的方法随机且低效，基于数学规划（MINLP）的方法计算昂贵且难以处理无闭式表达的性能度量。
- **意义**：提出一种既能保持符号可解释性、又能高效搜索的自动算法发现框架，为算法创新提供新范式。

### 2. 论文提出的方法论
- **核心思想**：将更新函数表示为表达式树（离散空间），利用变分自编码器（VAE）学习其连续潜在表示，再通过贝叶斯优化（BO）在连续空间中高效搜索，从而将离散组合优化转化为连续优化问题。
- **关键技术细节**：
    1. **表达式树表示**：深度为 \(d\) 的完全二叉树的每个节点可分配二元/一元运算符或操作数（如 \(x_k, \partial_k F, cst\)），并通过二进制变量 \(y_{on}\) 和连续常数 \(c_n\) 参数化。
    2. **VAE 学习连续表示**：将二进制变量转换为整数编码 \(\hat{y}\)，编码器输出高斯分布参数 \((\mu, \sigma^2)\)，通过重参数化采样潜在向量 \(z\)，解码器重建整数表示。损失函数为重建交叉熵 + \(\beta \cdot \text{KL}\) 散度。
    3. **贝叶斯优化搜索**：以高斯过程（Matern核）为代理模型，使用 Log Expected Improvement 获取函数。在每次BO迭代中，优化获取函数找到候选 \(z\)，解码为更新函数 \(\hat{y}\)，若无效则赋予高惩罚；否则求解内层常数优化问题（式9），得到性能度量 \(m(\hat{y})\)，更新GP。
    4. **常数优化**：给定函数形式，求解关于常数 \(c\) 的非线性规划，最小化在多个函数与起点上的平均梯度范数。

### 3. 实验设计
- **场景与数据集**：
    - **案例1**：两个非凸函数 \(F_1, F_2\)（四次函数等），各取两个随机起点（固定种子1234）。
    - **案例2**：线性回归 \(\min 0.5\|Ax-b\|^2\)，两个随机生成的 \((A,b)\) 系统，各两个起点。
- **基准方法**：梯度下降（GD），步长通过优化调优。
- **对比方法**：MINLP公式（文献[6]），分别用全局求解器 BARON 和 SCIP 求解（限制3600秒）。
- **实验设置**：表达式树深度 \(d=2\)（7节点），运算符集 \(\{+, -, *\}\)，操作数集 \(\{x_k, \partial_k f, cst\}\)。VAE用5000个随机表达式树训练（4000训练），隐藏维度经灵敏度分析设为7。BO初始随机点10或20个，迭代21或25次，总评估次数31或45次。

### 4. 资源与算力
- **明确提及**：VAE训练时间约1.66秒（CPU），未使用GPU。BO搜索总时间：案例1为121秒（其中112秒用于评估），案例2为176秒（157秒评估）。MINLP方法在3600秒内未收敛到更好解。
- **未说明**：具体CPU型号、内存等。所有计算均表明可在普通CPU上快速完成。

### 5. 实验数量与充分性
- **实验数量**：共2个案例，每个案例含2个函数×2个起点。未做大规模随机或更多函数集的实验。敏感性分析仅针对潜在空间维度。
- **充分性与公平性**：
    - 实验设计较简单但能证明方法可行性，对比了两种主流全局优化求解器（BARON、SCIP），且公平设定相同时间限制。
    - 但缺乏：①与遗传编程、LLM方法对比；②更多基准问题（如经典优化测试集）；③消融实验（如不同VAE结构、不同获取函数）、④统计显著性（仅单次运行）。
    - 结论明确但泛化性需更多验证。

### 6. 论文的主要结论与发现
- **新更新函数发现**：
    - 案例1：发现状态依赖步长规则 \(x_{i+1} = x_i - 0.035 (2.18 + x_i) \nabla F(x_i)\)，在5次迭代内梯度范数低于GD的12次迭代结果。
    - 案例2：发现带阻尼项规则 \(x_{i+1} = x_i - 0.11 \nabla F(x_i) - 0.00161 x_i\)，对病态问题表现更好。
- **性能提升**：所提方法在计算时间（~120-176秒）上远优于MINLP方法（3600秒未获更好解）。
- **可解释性**：符号形式便于分析，如GD也被再现。

### 7. 优点
- **可解释性与通用性**：以表达式树表示、符号形式输出，无需预设函数形式，可推广到其他迭代算法（方程求解、PDE等）。
- **计算高效**：利用VAE降维+BO的样本效率，总时间远低于数学规划方法，且训练开销可忽略（~1.66秒）。
- **方法创新**：首次将VAE与BO结合用于迭代算法发现，巧妙化解离散与连续搜索的矛盾。
- **处理黑盒评价**：BO框架天然适用于性能度量无闭式表达的情况（如求解时间）。

### 8. 不足与局限
- **实验规模小**：仅两个案例，各含2个函数，缺乏大规模、多样化基准测试，结论泛化性存疑。
- **潜在偏差**：VAE训练数据为随机有效树，可能未覆盖所有有用结构；BO初始点随机，可能影响发现结果。
- **计算依赖**：内部常数优化需要求解全局非线性规划，对于更复杂或高维问题可能耗时或不可行；若使用局部优化可能降低性能。
- **有效性风险**：解码器可能输出无效树（文中未遇到），但理论上需处理；常数优化可能无解（发散），用大常数代替可能误导搜索。
- **应用限制**：表达式树深度固定（d=2），限制了算法复杂度；仅考虑梯度信息，未扩展到更高阶或更复杂算子。

（完）
