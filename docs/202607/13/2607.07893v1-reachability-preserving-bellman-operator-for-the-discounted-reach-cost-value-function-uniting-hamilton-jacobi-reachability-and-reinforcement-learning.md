---
title: "Reachability-Preserving Bellman Operator for the Discounted Reach-Cost Value Function: Uniting Hamilton-Jacobi Reachability and Reinforcement Learning"
title_zh: 保持可达性的折扣可达成本值函数的贝尔曼算子：统一哈密顿-雅可比可达性与强化学习
authors: "Isabelle El-Hajj, Prashant Solanki, Jasper van Beers, Coen de Visser, Erik-Jan van Kampen"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07893v1"
tags: ["query:lyapunov-ai"]
score: 8.0
evidence: 结合HJ可达性与强化学习，保障动力系统安全
tldr: Hamilton-Jacobi可达性提供严格安全保障但计算困难，强化学习可扩展但面临非加性和语义损失问题。本文基于保留原语义的折扣可达代价价值函数，推导非加性Bellman算子并证明其压缩性，从而保证不动点存在唯一且与HJ等价。数值实验验证了零水平集对齐，为高维系统数据驱动安全分析建立了原理性桥梁。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07893v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 722, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07893v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 734, \"height\": 1198, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07893v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 725, \"height\": 590, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07893v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 716, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07893v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 587, \"height\": 577, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07893v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 831, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07893v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 936, \"height\": 969, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07893v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 824, \"height\": 539, \"label\": \"Table\"}]"
motivation: 弥合HJ可达性与强化学习之间的语义鸿沟，使样本方法能逼近可达性价值函数而不失真。
method: 提出保留可达性语义的折扣价值函数及其对应的非加性Bellman算子，并证明其压缩性。
result: 证明算子的唯一不动点与HJ解一致，价值迭代收敛，数值实验验证零水平集对齐。
conclusion: 为高维系统可扩展、数据驱动的可达集与安全证书计算提供理论框架。
---

## 摘要
哈密顿-雅可比（HJ）可达性为连续时间动力系统提供了严格的安全性与可达性保证，但其数值求解受维数灾难的困扰。相比之下，深度强化学习（DRL）提供了可扩展的基于样本的方法。然而，强化学习通常基于加性累积奖励构建，而可达性目标本质上是非加性的。这种不匹配使得直接在HJ可达性与强化学习之间建立桥梁颇具挑战。近年来的折扣公式要么通过改变原始可达性语义引入压缩性，要么在HJ侧保留精确语义但缺乏对应的贝尔曼不动点刻画。本文通过建立一种保留语义的折扣可达值函数，并推导出一个非加性贝尔曼算子，其唯一不动点与HJ公式中的值函数精确匹配，从而弥合了这一差距。我们证明折扣使该算子具有压缩性，从而确保了值迭代的存在性、唯一性与收敛性。此外，我们建立了HJ刻画与贝尔曼刻画之间的等价性，并表明强化学习可被解释为同一不动点方程的基于样本的近似方案。这就在HJ可达性与强化学习之间建立了原则性且语义精确的连接，使得基于学习的方法能够在保留其安全关键含义的同时近似可达性值函数。因此，所提出的框架为高维系统中可达集与安全证书的可扩展、数据驱动计算打开了大门。数值实验表明与HJ解高度吻合，通过零水平集对齐确认了可达性语义的保留，并支持将强化学习解释为所提出的贝尔曼算子的基于样本求解器。

## Abstract
Hamilton-Jacobi (HJ) reachability provides rigorous safety and reachability guarantees for continuous-time dynamical systems, but its numerical solution suffers from the curse of dimensionality. Deep reinforcement learning (DRL), by contrast, offers scalable sample-based methods. However, RL is typically built around additive cumulative rewards; whereas, reachability objectives are inherently non-additive. This mismatch makes a direct bridge between HJ reachability and RL nontrivial. Recent discounted formulations have either introduced contraction by altering the original reachability semantics, or preserved exact semantics on the HJ side without a corresponding Bellman fixed-point characterization. In this paper, we close this gap by building on a semantics-preserving discounted reach-based value function and deriving a non-additive Bellman operator whose unique fixed point exactly matches the value function in the HJ formulation. We prove that discounting makes this operator contractive, yielding existence, uniqueness, and convergence of value iteration. Furthermore, we establish the equivalence between the HJ and Bellman characterizations, and show that RL can be interpreted as a sample-based approximation scheme for the same fixed-point equation. This yields a principled and semantically exact connection between HJ reachability and RL, enabling learning-based methods to approximate reachability value functions while preserving their safety-critical meaning. As a result, the proposed framework opens the door to scalable, data-driven computation of reachable sets and safety certificates in high-dimensional systems. Numerical experiments demonstrate close agreement with HJ solutions, confirm preservation of reachability semantics via alignment of zero level sets, and support the interpretation of reinforcement learning as a sample-based solver of the proposed Bellman operator.

---

## 论文详细总结（自动生成）

# 中文总结：Reachability-Preserving Bellman Operator for the Discounted Reach-Cost Value Function

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：Hamilton-Jacobi (HJ) 可达性分析为连续时间动力系统提供严格的安全性与可达性保证，但其数值求解受“维数灾难”困扰，难以扩展到高维系统。深度强化学习（DRL）具有可扩展性，但传统的 RL 基于加性累积奖励，而可达性目标本质上是非加性的（取决于是否进入目标集，而非路径奖励之和），因此两者之间存在语义鸿沟。
- **背景挑战**：已有工作要么通过修改原始可达性语义来获得压缩性（适合 RL），要么在 HJ 侧保留精确语义但缺乏对应的 Bellman 不动点刻画。缺少一个既能保留语义又能与 RL 兼容的统一框架。
- **核心目标**：弥合 HJ 可达性与 RL 之间的鸿沟，提出一种保留语义的折扣可达值函数及其对应的非加性 Bellman 算子，使得 RL 可被解释为该算子的样本近似求解器，且仍保持安全关键含义。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：基于 Choi 等人提出的语义保留折扣可达值函数 \( V(t,x) = \inf_{u(\cdot)} \inf_{s\in[t,T]} g(\xi(s)) e^{-\lambda (s-t)} \)（其中 \(g\) 为可达代价，\(g<0\) 表示在目标集内），推导出对应的动态规划原理（DPP）和非加性 Bellman 算子 \( T_{\sigma,\lambda} \)。该算子采用“停止分支”（检测是否已进入目标集）和“延续分支”（折扣后的后续函数值）的 min 结构。
- **关键技术细节**：
  - 定义时间-剩余值函数 \( W(\tau,x) \) 和无限时域版本 \( W_\infty(x) \)。
  - 构造 Bellman 算子 \( (T_{\sigma,\lambda}\Psi)(\tau,x) = \inf_{u} \min\left\{ \inf_{r\in[0,h(\tau)]} e^{-\lambda r} g(\phi_u(r)), e^{-\lambda h(\tau)}\Psi(\tau-h(\tau), \phi_u(h(\tau))) \right\} \)，其中 \( h(\tau)=\min(\tau,\sigma) \)。
  - 证明该算子在 sup-norm 下是压缩的（收缩因子 \(e^{-\lambda\sigma}<1\)），利用 Banach 不动点定理确保存在唯一不动点，该不动点正是原值函数。
  - 建立 HJ 粘度解刻画（HJVI）与 Bellman 不动点刻画的等价性（Theorem 7, 8）。
  - 提出离散时间近似算子 \( \hat T_{\sigma,\lambda} \)，并证明其收敛到连续算子（Theorem 9, 10）。
  - 提出拟合值迭代（FVI）算法（Algorithm 1, 2），将 RL 解释为样本近似：在每个采样状态，通过模型 roll-out 计算 Bellman 目标（min 结构），然后回归拟合神经网络。
- **算法流程（文字说明）**：
  - 有限时域情形（Algorithm 1）：先用 g(x) 预训练网络以满足边界条件 \( \Psi(0,x)=g(x) \)；然后在每次迭代中采样状态-时间对，对每个控制计算 H 步动力学 roll-out，计算停止代价最小值与延续代价（使用当前网络对 \( \tau-\sigma \) 时刻的估计），取 min 作为目标值；最小化均方 Bellman 残差更新网络参数。
  - 无限时域情形（Algorithm 2）：无预训练步骤，直接进行类似的 Bellman 目标计算（roll-out 固定 H 步），网络只接收状态输入。

## 3. 实验设计：使用的数据集 / 场景、benchmark、对比方法
- **场景**：
  1. **双积分器（Double Integrator）到达问题**：状态 \( (x_1,x_2) \)，控制 \( u\in[-1,1] \)，目标集为半径为 0.5 的圆。可达代价 \( g \) 采用缩放后的 tanh 符号距离函数。
  2. **Dubins 车（Dubins Car）避障问题**：状态 \( (x,y,\theta) \)，速度恒定 1 m/s，控制 \( \dot{\theta}\in[-1,1] \)，避免半径为 0.5 的圆形区域。同样使用 tanh 可达代价。
- **Benchmark**：对无限时域情形，将 HJ 粘度解的参考解（通过半拉格朗日格式求解静止 HJVI）作为地面真实值。对于 Dubins 车避障，还使用 helperOC 工具箱计算**无折扣**值函数作为语义对比基准。
- **对比方法**：本文所提的 FVI 算法（学习值函数）与参考值函数进行点对点误差对比，以及零水平集（决定可达/安全集边界）对比。没有与其他 RL 方法（如 MDR、DeepReach）进行定量比较。

## 4. 资源与算力
论文正文**未明确提及**使用的 GPU 型号、数量或训练时长。仅给出网络结构和超参数：SIREN 网络（双积分器：隐藏层 64×2；Dubins：128×2）、学习率 1e-4（双积分器）或 2.87e-4（Dubins）、批次大小 16384、训练步数 200,000。推断使用了中等规模的算力，但无详细信息。

## 5. 实验数量与充分性
- **实验数量**：两个场景（双积分器到达、Dubins 车避障）。每个场景展示了一组收敛后的值函数图、误差图和零水平集对比图。
- **充分性与客观性**：
  - **充分性**：实验成功验证了 HJ-Bellman 等价性（点对点误差小）和语义保留（零水平集对齐），证明了方案的有效性。但未进行消融实验（如不同折扣率、不同 Bellman 步长的影响），也未在高维系统（例如 >4 维）上测试可扩展性。
  - **公平性**：对比了参考解（半拉格朗日格式），属于标准基准；但未与 MDR、DeepReach 等方法直接比较性能，因此难以评估相对于同类方法的优势。实验设计偏向于验证理论，而非全面评估实用性能。

## 6. 论文的主要结论与发现
- **主要发现**：
  - 所提出的非加性 Bellman 算子是压缩的（收缩因子 \( e^{-\lambda\sigma} \)），其唯一不动点精确等于 HJ 粘度解定义的值函数，且价值迭代收敛。
  - HJ 刻画与 Bellman 刻画等价，RL 可被解释为同一不动点方程的样本近似求解器，从而建立了原则性且语义精确的桥梁。
  - 数值实验证实：学习值函数与 HJ 参考解高度一致（双积分器误差 < 0.0015），零水平集完美对齐，表明折扣不改变可达性语义。
- **结论**：该框架为高维系统可扩展的安全分析提供了理论基础，允许使用数据驱动方法计算可达集和安全证书而不丢失语义。

## 7. 优点：方法或实验设计上的亮点
- **理论贡献突出**：首次证明了保留语义的折扣可达值函数满足压缩的 Bellman 算子，融合了 HJ 的严格性与 RL 的可扩展性。
- **语义保真**：折扣因子仅影响值函数大小，不改变零水平集，因此可达集/安全集边界与无折扣情形一致，这是与 MDR 等方法的本质区别。
- **形式简洁**：Bellman 算子仅需 min 和 inf 操作，易于实现；离散化收敛性证明完备（Theorem 9, 10）。
- **实验验证充分**：通过点对点误差和零水平集对齐双重验证，结果直观有力。

## 8. 不足与局限
- **理论局限**：函数近似（FVI）下的收敛性未证明（Remark 8 明确指出这是一个开放问题）。当前只能保证对精确 Bellman 算子的收敛，但实际使用神经网络近似时，误差传播可能破坏压缩性。
- **实验局限性**：
  - 仅测试 2D 和 3D 低维系统，未展示在高维（如 10 维）上的有效性，无法验证可扩展性声称。
  - 缺乏与现有方法（如 MDR、DeepReach）的性能对比，难以评估实用优劣。
  - 未进行消融研究（如不同 \(\lambda\)、\(\sigma\)、网络容量等的影响）。
  - 实验只涉及到达和避障两类问题，未覆盖更复杂的“到达-避障”混合问题。
- **其他限制**：算法假设已知控制空间离散化，且需要精确的动力学模型进行 roll-out，不完全符合无模型 RL 场景。论文虽提及“学习策略”可替代优化，但未深入探讨。

（完）
