---
title: Discovering Latent Response Laws in Forced Physical Systems
title_zh: 发现受迫物理系统中的潜在响应规律
authors: "Yi Zhu, Su Chen, Xiaojun Li, Xiuli Du"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.09801v1"
tags: ["query:sr"]
score: 8.0
evidence: 从受迫物理系统数据中发现潜在控制方程
tldr: 强制物理系统的高维观测隐藏了简洁的动力学方程，难以直接发现。FLARE通过潜变量自编码器学习紧凑响应坐标，识别稀疏输入依赖的潜动力学。在多个已知和应用规模系统中，该方法成功恢复强制动力学并预测未见输入下的长时间高维响应。这为有效状态隐藏在复杂观测中的系统提供了可解释建模与预测的新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09801v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1662, \"height\": 1325, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09801v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1674, \"height\": 1513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09801v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1664, \"height\": 1904, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09801v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1683, \"height\": 1317, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09801v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1675, \"height\": 1341, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09801v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1398, \"height\": 1503, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09801v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1641, \"height\": 281, \"label\": \"Table\"}]"
motivation: 高维测量中强制物理系统的简洁响应方程难以发现，需分离状态估计与外部强迫。
method: FLARE模型学习紧凑响应坐标，识别稀疏输入依赖的潜动力学并解码方程滚到完整响应。
result: 在已知系统、应用规模强制响应和视觉观测中，FLARE恢复紧凑强制动力学并预测未见输入下的长时间高维响应。
conclusion: FLARE将方程发现扩展到有效状态隐藏在复杂观测中的系统，实现可解释建模与预测。
---

## 摘要
控制方程提供了物理系统的简洁描述，但其中变量简单的形式往往隐藏在高维测量数据中。对于受迫系统而言，这一挑战更为突出，因为其响应既依赖于内在动力学，也依赖于随时间变化的输入。本文提出了FLARE（受迫响应方程潜变量自编码器），它学习紧凑的响应坐标，识别稀疏的输入依赖潜动力学，并将方程演化解码为完整响应。通过从数据中估计潜变量维度，并将状态估计与外部强迫分离，FLARE使得预测能够从过去的响应初始化，并由预设的未来输入驱动。在已知动力学系统、应用规模的受迫响应以及视觉观测中，FLARE恢复了紧凑的受迫动力学，并在未用于训练的输入下预测了长期的高维响应。通过将学习到的坐标转化为动力学接口，FLARE将方程发现扩展到有效状态隐藏在复杂观测中的系统，为受迫动力学系统中高维响应的可解释建模与预测提供了一条路径。

## Abstract
Governing equations provide compact descriptions of physical systems, yet the variables in which they are simple are often hidden in high-dimensional measurements. This challenge is sharper for forced systems, whose responses depend on both intrinsic dynamics and time-dependent inputs. Here we introduce FLARE, a forced latent autoencoder for response equations that learns compact response coordinates, identifies sparse input-dependent latent dynamics and decodes equation rollouts to full responses. By estimating latent dimension from data and separating state estimation from external forcing, FLARE enables forecasts to be initialized from past responses and driven by prescribed future inputs. Across known dynamical systems, application-scale forced responses and visual observations, FLARE recovers compact forced dynamics and predicts long-horizon high-dimensional responses under inputs not used for training. By turning learned coordinates into a dynamical interface, FLARE extends equation discovery to systems whose effective states are hidden within complex observations, providing a route for interpretable modelling and prediction of high-dimensional responses in forced dynamical systems.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机与背景）

*   **核心问题**：受迫物理系统的响应同时依赖于内在动力学和时变外部输入。然而，系统有效状态的简洁描述变量往往隐藏在高维观测（如传感器阵列、视频）中，传统方程发现方法难以直接应用。
*   **研究动机**：现有方法要么假设已知状态坐标，要么只处理自治系统，要么无法在统一框架下同时实现表示学习、稀疏方程发现和高维响应预测。对于受迫系统，尤其需要一种能从高维观测自动发现紧凑、输入依赖的响应方程的方法。
*   **整体含义**：FLARE框架将受迫系统的响应方程发现转化为一个结合潜变量自编码器和稀疏系统辨识的联合优化问题，使得模型可以在初始观测历史后仅由预设输入驱动，从而在未见输入下预测长时间高维响应，实现可解释建模。

## 2. 论文提出的方法论

*   **核心思想**：通过一个潜变量自编码器学习紧凑的响应坐标（潜状态），并同时识别一个稀疏的、显式包含外部输入的潜动力学方程；将该方程滚动后解码回完整高维响应。
*   **关键技术细节**：
    *   **架构组件**：
        *   **编码器** \( z_k = E_\theta(W_k) \)：从长度为 \( L \) 的因果响应窗口 \( W_k = (x_{k-L+1}, \ldots, x_k) \) 提取潜状态，**不包含外部输入**。
        *   **潜响应方程**：\( \dot{z}(t) = \Theta(z(t), u(t)) \Xi \)。候选函数库 \( \Theta \) 由潜变量、外部输入及其交互项组成，\( \Xi \) 是待辨识的稀疏系数矩阵。
        *   **解码器** \( \hat{x}_k = D_\phi(\hat{z}_k) \)：将潜状态映射回高维观测。
    *   **潜变量维度估计**：预训练阶段后，对潜变量点云使用三种互补估计器（LB-MLE、TwoNN、PCA）自动选择维度。
    *   **三步训练流程**：
        1.  **预训练**：仅用重构损失 \( \mathcal{L}_{pre} \) 训练编码器-解码器，获得初始潜点云并估计维度。
        2.  **正式训练**：联合优化编码器、解码器和稀疏系数矩阵。总损失 \( \mathcal{L}_{formal} \) 包括：
            *   响应重构损失（保局部解码质量）
            *   解码滚动损失（要求方程滚动后与真实观测一致）
            *   潜滚动损失（对齐滚动与编码轨迹，用 stop-gradient 防止退化）
            *   方程一致性损失（拟合局部导数）
            *   \( \ell_1 \) 正则化促进稀疏性
        3.  **后训练**：固定编码器和方程系数，仅微调解码器，提升滚动轨迹的重构精度。
    *   **视频扩展**：将全连接编码器/解码器替换为卷积版本（空间卷积 + 因果时间卷积），保留相同的输入依赖潜方程。

## 3. 实验设计

*   **数据集/场景**：
    *   **合成系统**（已知真实方程）：
        *   受迫阻尼摆（1维状态，1维输入）
        *   受迫Hopf振荡器（2维状态，2维输入）
        *   受迫刚体旋转（3维状态，3维输入）
        *   每个系统通过非线性观测映射到64维向量（正弦展开或多项式混合）。
    *   **应用规模系统**：
        *   薄板加热：2个独立加热器驱动，100个空间温度测点。
        *   机器人操纵器（Franka Emika Panda）：4个关节位置指令作为输入，105维标记点坐标作为响应。
        *   结构地震响应：2层框架Bouc-Wen滞回模型，地面加速度输入，36个水平位移输出。
    *   **视频观测**：
        *   受迫Van der Pol振荡器（32×32灰度视频）
        *   受迫悬臂梁（32×32灰度视频）
        *   受迫圆柱尾流（LBM模拟，32×96灰度视频）
    *   **未见输入测试**：每个应用系统构建10组与训练输入在时序、频谱或跨通道协调上不同的新输入轨迹，不用于训练/验证/模型选择。

*   **基准方法**：
    *   DMDc（线性/koopman基线）
    *   Deep Koopman with control
    *   POD-SINDYc（投影+稀疏辨识）
    *   AE-SINDYc（自编码器+稀疏辨识）
    *   CT-SUBNET（非线性状态空间辨识）
    *   TiDE、TFT、TCN、GRU（通用时序预测模型）
    *   所有基线使用相同的训练/验证/测试/未见输入划分，相同的初始观测窗口和预设输入，相同的滚动时长和评价指标。

*   **评价指标**：NRMSE（向量/字段），SSIM（视频）。

## 4. 资源与算力

*   论文明确说明：“All experiments were conducted on a workstation equipped with an **NVIDIA Tesla V100 GPU**。”
*   **未提及**：具体GPU数量、训练总时长、模型参数量级等详细信息。

## 5. 实验数量与充分性

*   **实验数量**：
    *   合成系统3个（每个含100条轨迹，80/10/10划分）。
    *   应用系统3个（每个含训练/验证/测试集，以及10组未见输入测试）。
    *   视频任务3个（每个80/10/10划分，Van der Pol和悬臂梁400帧，圆柱500帧）。
    *   对比9种基线，在3个应用系统上均报告了25%、50%、100%滚动时长的NRMSE。
    *   每个消融？文中未独立做消融实验（如去掉某个损失项），但有分阶段训练的对比意义。
*   **充分性与公平性**：
    *   实验覆盖了合成已知方程、应用实际系统、视频观测三种难度级别，具有较好代表性。
    *   基线选择覆盖了线性、koopman、稀疏辨识、状态空间、通用时序模型，比较全面。
    *   评价协议统一：所有模型在同一初始化条件下滚动预测，使用相同指标。
    *   未见输入测试进一步检验泛化能力，避免了过拟合风险。
    *   **不足**：缺乏对潜维度选择敏感性、不同候选库设计、不同训练阶段贡献的系统性消融研究。同时，视频任务未与视频预测基线（如ConvLSTM）对比。

## 6. 论文的主要结论与发现

*   在合成系统上，FLARE能成功恢复已知受迫方程（经仿射变换对齐后），且不要求潜坐标与物理坐标逐项对应。
*   在三个应用规模系统上，FLARE在**未见输入**下仍能保持较低NRMSE（薄板3.0%，机器人1.6%，结构13.7%），且优于所有基线（尤其在长时间滚动和泛化方面）。
*   DMDc在薄板加热上表现优秀，但其他任务不稳定甚至发散；GRU等时序模型在短时表现不错但长时退化；FLARE在多种场景下均保持稳定。
*   在视频任务上，FLARE能学到潜坐标中的简洁方程（如Van der Pol恢复二阶方程），并解码出高保真度视频（SSIM在0.88~0.98）。
*   **核心发现**：将状态估计与外部强迫分离，并强制潜空间满足稀疏输入依赖动力学，能提供更强的归纳偏置，从而稳定长时滚动、支持泛化。

## 7. 优点

*   **方法设计**：
    *   创新地将稀疏系统辨识与自编码器结合，并统一处理受迫系统的高维响应预测。
    *   自动估计潜变量维度，避免手动调参过度或不足。
    *   三步训练流程（预、正式、后训练）有效稳定了潜空间和方程学习，避免退化。
    *   视频扩展灵活，保持相同潜方程框架，仅更换编码/解码器结构。
    *   损失函数设计周全（重构、滚动、一致性、稀疏性），并巧妙使用stop-gradient。
*   **实验验证**：
    *   合成系统验证了方程发现的正确性（经坐标对齐后）。
    *   应用系统覆盖不同的物理机制（扩散、机械、结构地震），具有实际意义。
    *   未见输入测试是强泛化检验，比常规测试更具说服力。
    *   与9种广泛使用的基线进行统一比较，结果显示FLARE在长时间滚动和泛化上显著优于其他方法。
    *   视频任务从像素直接学习，验证了方法对视觉模态的适用性。

## 8. 不足与局限

*   **方法局限**：
    *   依赖候选函数库 \( \Theta \) 的预设，库外项无法被发现，可能限制复杂非线性关系的恢复。
    *   潜坐标与物理坐标之间只保证仿射变换等价，不保证坐标可解释性（例如潜变量可能没有明确的物理意义）。
    *   性能依赖于观测信息量、输入覆盖范围和潜维度估计的准确性。
    *   未考虑不确定性量化、守恒律或对称性先验的融入。
*   **实验局限**：
    *   **缺乏消融研究**：未系统分析各损失项、不同潜维度估计器、不同候选库对性能的影响。
    *   **视频对比不足**：未与ConvLSTM、PredRNN等专为视频预测设计的基线对比，削弱了视频部分的说服力。
    *   **算力信息不全面**：只说明了GPU型号，未提供训练时间、参数量等，不利于复现和比较效率。
    *   **未见输入泛化测试**：虽然构造了10组不同输入，但未明确描述这些输入与训练输入分布的距离（如是否真的外推到了训练数据范围之外），可能导致低估过拟合风险。
    *   **应用规模系统的滚动长度**均为500步，未探讨更长预测时的误差累积情况。
*   **应用限制**：
    *   要求响应历史能够提供足够的初始状态估计，对于严重缺失或噪声极大的观测可能失效。
    *   需要成对的输入-响应数据，纯无监督或输入部分未知的场景不适用。
    *   对于混沌或极高维（如高分辨率3D流体）系统，潜维度估计和方程辨识可能面临可扩展性挑战。

（完）
