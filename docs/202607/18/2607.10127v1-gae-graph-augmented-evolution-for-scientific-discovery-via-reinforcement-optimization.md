---
title: "GAE: Graph-Augmented Evolution for Scientific Discovery via Reinforcement Optimization"
title_zh: GAE：通过强化优化实现科学发现的图增强进化
authors: "Xuanzhou Chen, Taoli Cheng"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10127v1"
tags: ["query:sr"]
score: 8.0
evidence: 利用大语言模型和强化学习优化的进化程序搜索用于科学发现
tldr: 现有LLM引导的进化程序搜索面临亲本选择盲目、奖励稀疏和变异算子静态等瓶颈。GAE框架通过图神经网络解析计算图、强化学习元控制器定向选择与变异、在线GRPO持续优化算子来解决。在复杂非线性振荡器符号回归任务中高效发现闭式方程，性能匹配或超越基线，实现最先进分布外泛化。该工作将随机搜索转化为定向自改进过程，推动科学发现自动化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10127v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 838}, {\"url\": \"assets/figures/arxiv/2607.10127v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 532}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10127v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 444}, {\"url\": \"assets/tables/arxiv/2607.10127v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 917, \"height\": 281}, {\"url\": \"assets/tables/arxiv/2607.10127v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1502, \"height\": 214}, {\"url\": \"assets/tables/arxiv/2607.10127v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 324}, {\"url\": \"assets/tables/arxiv/2607.10127v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1059, \"height\": 1344}]"
motivation: 解决LLM进化搜索中亲本选择盲目、奖励稀疏和变异算子静态三大瓶颈。
method: 提出GAE框架，结合图神经网络、强化学习元控制器和在线GRPO微调，实现结构化感知与自适应变异。
result: 在非线性振荡器符号回归中，GAE发现闭式物理方程，性能匹配或超越基线，分布外泛化达SOTA。
conclusion: GAE将进化搜索转化为定向自改进过程，有效提升科学发现效率与泛化能力。
---

## 摘要
由大语言模型（LLM）引导的进化程序搜索已成为自动化科学发现的强大范式。然而，当前方法受到三个瓶颈的根本性约束：结构盲目的父代选择、稀疏的整个程序评估奖励，以及在搜索过程中无法适应的静态变异算子。我们提出GAE（图增强进化），该框架通过紧密耦合的三支柱架构解决了这些局限性。首先，关系图神经网络（GNN）将程序解析为类型化计算图，产生结构感知的嵌入。其次，一个RL优化的元控制器利用这些嵌入将盲目进化采样替换为定向策略，基于奖励历史动态选择最优父代和变异方向。第三，在线GRPO微调循环使用组归一化评估奖励在测试时持续更新LLM变异算子，直接将模型的生成分布与高适应度结构编辑对齐。我们在具有挑战性的科学发现任务——复杂非线性振荡器系统的符号回归上评估GAE。通过将随机搜索转变为定向的自改进轨迹，GAE高效发现闭合形式的物理方程，持续匹配或超越静态LLM驱动基线，并在分布外性能上达到最优。

## Abstract
Evolutionary program search guided by Large Language Models (LLMs) has emerged as a powerful paradigm for automated scientific discovery. However, current approaches are fundamentally constrained by three bottlenecks: structurally blind parent selection, sparse whole-program evaluation rewards, and static mutation operators that fail to adapt during search. We present GAE (Graph-Augmented Evolution), a framework that resolves these limitations through a tightly coupled, three-pillar architecture. First, a relational graph neural network (GNN) parses programs into typed computation graphs, producing structure-aware embeddings. Second, an RL-optimized meta-controller leverages these embeddings to replace blind evolutionary sampling with a directed policy, dynamically selecting optimal parents and mutation directions based on reward history. Third, an online GRPO fine-tuning loop continuously updates the LLM mutation operator at test-time using group-normalized evaluation rewards, directly aligning the model's generation distribution with high-fitness structural edits. We evaluate GAE on a challenging scientific discovery task: symbolic regression for complex nonlinear oscillator systems. By transforming stochastic search into a directed, self-improving trajectory, GAE efficiently discovers closed-form physical equations, consistently matching or outperforming static LLM-driven baselines and achieving state-of-the-art out-of-distribution performance.

---

## 论文详细总结（自动生成）

# GAE: Graph-Augmented Evolution for Scientific Discovery via Reinforcement Optimization 中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究背景**：大语言模型（LLM）引导的进化程序搜索已成为自动化科学发现的重要范式。典型工作如 FunSearch、AlphaEvolve、OpenEvolve 等，通过迭代搜索框架，让 LLM 作为程序变异算子，结合进化算法和 MAP-Elites 质量多样性机制，在数学发现、代码优化等任务上取得突破。
- **核心瓶颈**：现有方法仍受三个根本性限制：
  - **奖励稀疏**：每个程序仅以一个完整评估分数（如 NMSE）作为信号，导致大量部分正确但有价值的结构被丢弃（如接近最优但未严格超越的程序）。
  - **父代选择盲目**：常用均匀采样或基于适应度的随机选择，忽略了程序之间的结构相似性，导致搜索过程接近随机探索，缺乏局部结构引导。
  - **变异算子静态**：LLM 的生成权重在搜索过程中固定不变，不能根据搜索历史自适应调整编辑分布，限制了探索效率。
- **研究意义**：该工作旨在通过同时解决上述三个瓶颈，将随机搜索转化为**定向、自改进的进化轨迹**，提升科学发现的效率和泛化能力。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出 **GAE（Graph-Augmented Evolution）** 框架，在 OpenEvolve 骨干上集成三个紧密耦合的组件：关系图神经网络（GNN）编码器、离散 Soft Actor–Critic（SAC）元控制器、GRPO 在线微调循环。三者分别负责结构感知嵌入、定向变异选择、变异算子在线自适应。
- **关键技术细节**：
  1. **GNN 编码器（§3.3）**：
     - 将每个程序（符号回归表达式）解析为**类型化计算图**（节点为数学操作，边为数据流），使用关系图卷积网络（Relational GCN）生成 128 维结构嵌入 `z ∈ ℝ^d`。
     - 包括输入投影、关系消息传递（L=2 层）、全局拼接池化。
     - 预训练后，在进化过程中每 K=3 代微调一次（使用最近 300 个样本的（嵌入，奖励）对进行 MSE 回归），使嵌入跟踪程序分布变化。
     - 公式（1）-（5）详细定义了图构建与编码过程。
  2. **SAC 元控制器（§3.4）**：
     - **状态**：父程序嵌入 `z_α`。
     - **动作空间**：2700 个离散动作，每个动作编码 (变异类型, 目标节点 ID, 参数索引)。变异类型包括：add operator、replace operator、delete node、insert constant、subtree rewrite。
     - **策略**：离散 SAC，三隐藏层 MLP（256 维），自动调节熵权重 α，目标熵设为 `-½ log|A|`。
     - **奖励**：由三部分组成：
       - 任务奖励 `r_task = clip(R(β), r_min, r_max)`（最佳子代绝对分数）
       - 新颖性奖励 `r_novelty = 1 - min_p cos(z_β, z_p)`（嵌入空间中与种群最大余弦相似度的差值）
       - 复杂度惩罚 `r_cpx = -λ2 * max(0, (N_nodes - 20)/20)`
     - 训练：优先经验回放，经过 32 步预热后，每代使用 K 个梯度步更新双 Q 网络和策略。
  3. **GRPO 在线微调（§3.5）**：
     - 每个父代生成 `G = 8` 个子代（同一组），计算各子代相对于父代的改进奖励 `R_g^grp = R_g - R_α`。
     - 计算组归一化优势 `Â_g = (R_g^grp - mean(R_grp)) / std(R_grp + ε)`。
     - 进行 PPO-clip 更新（ε=0.2），并加上 KL 散度正则化（β=0.1）对抗参考模型。
     - 只对得分高于 -10 的子代执行更新；若组标准差 < 1e-8 则跳过更新。
- **算法流程**（Algorithm 1）：
  - 每代开始：从固定大小精英种群中均匀采样父代
  - 使用 GNN 计算父嵌入 `z_α`
  - SAC 策略（预热后）选择动作 a（变异类型提示）
  - LLM 生成 G 个子代代码
  - 评估每个子代（数值积分+BFGS 优化常数）
  - 执行 GRPO 更新 LLM 权重（全权重更新，学习率 1e-6，AdamW）
  - 选择最佳子代用于 SAC 过渡存储（状态、动作、奖励、下一状态）
  - 每 K 代微调 GNN 并刷新所有种群嵌入
- **组件独立性**：SAC、GRPO、GNN 优化不相交的参数集，以不同时间尺度运行，可独立替换或禁用。

## 3. 实验设计：数据集、基准方法与对比方法
- **任务**：复杂非线性振荡器系统的符号回归，预测加速度 `ẋ = dv/dt`，输入为位置 x、时间 t、速度 v。
- **数据集**：使用 **LLM-SRBench** 的子集 **LSR-Synth** 中的 `phys_osc` 领域（非线性谐振子）。提供训练集、分布内（ID）测试集、分布外（OOD）测试集（不同时间区域采样）。
- **评估指标**：`log₁₀(NMSE)`，数值常数通过 BFGS 优化得到。分数越低（负数绝对值越大）越好。
- **对比方法**（表1、表2）：
  - 非 LLM 基线：uDSR
  - LLM 引导进化基线：LLM-SR、OpenEvolve、CodeEvolve、ShinkaEvolve、PACEvolve（Single/Multi）
  - GAE 使用 **Qwen3.5-35B-A3B** 作为基础模型，运行时全权重在线微调。
- **预算设置**：
  - 高预算（对照表1）：1000 迭代、10 次独立运行（参照 PACEvolve 原报告）。
  - 低预算（对照表2）：150 迭代，固定计算预算，与 OpenEvolve（Qwen/Gemini 2.5 Flash）和 PACEvolve-Single（Gemini）对比。GAE 运行 3 次，报告均值和最佳值。

## 4. 资源与算力
- **文中未明确说明**使用的具体 GPU 型号、数量或训练总时长。仅提到 Qwen3.5-35B-A3B 作为 LLM 骨干，LLM 全权重在线微调（学习率 1e-6），但无详细算力消耗数据。
- 附录中提供了超参数表，但未提及硬件信息。

## 5. 实验数量与充分性
- **数量**：
  - 表1：10 次独立运行的高预算对比（仅引用已有结果，GAE 未直接参与？实际上 GAE 未列在表1，表1是 PACEvolve 原报告数据，GAE 是在表2的 150 迭代低预算设置下与 OpenEvolve 和 PACEvolve-Single 比较）。
  - 表2：GAE 运行 3 次（报告均值和最佳），OpenEvolve 和 PACEvolve 各 1 次。
  - 消融实验：文中声称三个组件各自可独立禁用后退化到简单基线，但**未提供明确的消融实验数据表格**。
  - 单次运行轨迹图（图2）展示 GAE 在 150 代内的收敛过程。
- **充分性与公平性**：
  - 实验规模相对有限（仅一个任务、低预算下 3 次运行）。对比时未控制所有基线使用相同迭代数（表2中 PACEvolve 和 OpenEvolve 各1次），统计可靠性一般。
  - 作者指出 PACEvolve 使用 Gemini 2.5 Flash，而 GAE 使用 Qwen3.5-35B-A3B，模型不同可能影响公平性（但均现代大模型）。
  - 缺乏对奖励设计、GNN 间隔、组大小等关键超参数的敏感性分析。
  - 没有在其他任务（如数学发现、算法发现）上的实验，泛化结论有限。

## 6. 论文的主要结论与发现
- **性能结果**：在低预算（150 代）下，GAE 在分布内 NMSE 达到 **-7.24**（最佳），分布外 OOD NMSE 达到 **-7.45**，**显著优于** OpenEvolve（Qwen ID: -3.88, OOD: -3.94）和 PACEvolve-Single（Gemini ID: -5.80, OOD: -6.10）。
- **发现**：GAE 高效发现了接近精确的闭式物理方程（最佳 NMSE ≈ 5.7×10⁻⁸）。图2显示快速初期改进（前 20 代达 -4.64），并在第 57-88 代持续突破，第 119 代达到最优。共记录 13 次不同改进。
- 每个最佳程序（GAE、OpenEvolve-Qwen、OpenEvolve-Gemini、PACEvolve）均被完整列出并做了物理意义解释，展示了自动发现方程的**可解释性**。
- **核心结论**：GAE 通过结构感知、定向变异选择和在线自适应变异算子，使进化搜索从随机变为自改进，在符号回归科学发现中达到 SOTA 分布外泛化。

## 7. 优点
- **方法创新性强**：首次将图神经网络、强化学习元控制器、GRPO 在线微调三个组件紧密整合，同时解决三个核心瓶颈，而非孤立的改进。
- **框架模块化**：三个组件优化不相交参数集，可独立替换或禁用，便于未来扩展和改进。
- **实验设计合理**：使用了两种预算设置（高预算对照文献、低预算自对比），在低预算下严格控制迭代次数，验证了方法在有限计算下的高效性。
- **结果展示充分**：给出了详细的定量比较和物理方程解释，可视化进化轨迹，并附录了完整的算法伪代码和超参数表，可复现性好。
- **符号回归任务的实用价值**：发现闭式方程对物理科学具有重要意义，GAE 自动发现方程中包含了物理上有意义的项（如阻尼、非线性恢复力、外部驱动力等）。

## 8. 不足与局限
- **实验覆盖有限**：仅在单个任务（非线性振荡器符号回归）上评估，缺乏对其他科学发现领域（如数学、算法、硬件调度）的验证。作者承认“依赖 Python AST 图结构，扩展到其他语言需要重新设计节点类型和关系”。
- **消融实验缺失**：没有提供将 SAC、GRPO、GNN 分别禁用后的定量消融结果，仅文字描述“退化到简单基线”，证据不够充分。
- **统计可靠性不足**：低预算下 GAE 仅 3 次运行，OpenEvolve 和 PACEvolve 各 1 次，且未提供置信区间或标准误。结论可信度打折。
- **计算成本与效率未量化**：未报告 GPU 小时数或时间开销，GRPO 全权重微调可能显著增加计算负担。作者在 Limitations 中承认“GNN 和强化优化带来额外开销”，但未提供实际效率对比。
- **基准模型不一致**：表2中 GAE 使用 Qwen3.5-35B-A3B，而 PACEvolve 和 OpenEvolve 使用 Gemini 2.5 Flash，不同 LLM 的能力差异可能影响比较公平性。
- **未讨论失败案例和敏感性**：当组奖励方差小时 GRPO 跳过更新，这种自适应策略可能在某些情况下导致学习停滞。未探索超参数（如 λ₁, λ₂, G, K 等）对性能的影响。
- **缺乏与其他动态算子方法的直接比较**：如 ThetaEvolve（在测试时 RL 微调 LLM）等同类工作未被纳入定量对比（虽在相关工作提及）。
- **泛化性受限**：图表示依赖 Python AST；对于非代码形式的表达式（如纯数学公式），可能需要转换；奖励设计高度任务特定。

（完）
