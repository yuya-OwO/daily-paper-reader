---
title: "GAE: Graph-Augmented Evolution for Scientific Discovery via Reinforcement Optimization"
title_zh: GAE：基于图增强进化的科学发现强化优化方法
authors: "Xuanzhou Chen, Taoli Cheng"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10127v1"
tags: ["query:sr"]
score: 9.0
evidence: LLM引导的进化搜索结合RL优化，用于科学发现，适用于符号回归
tldr: 当前基于LLM的进化程序搜索存在结构盲选、稀疏奖励和静态变异三大瓶颈。本文提出GAE框架，通过GNN解析程序图为结构感知嵌入，RL元控制器基于奖励历史定向选择父代和变异方向，在线GRPO微调持续更新LLM变异器。在复杂非线性振荡器符号回归任务中，GAE高效发现闭式物理方程，匹配或超越基线，实现最先进的分布外性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10127v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 838}, {\"url\": \"assets/figures/arxiv/2607.10127v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 532}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10127v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 444}, {\"url\": \"assets/tables/arxiv/2607.10127v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 917, \"height\": 281}, {\"url\": \"assets/tables/arxiv/2607.10127v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1502, \"height\": 214}, {\"url\": \"assets/tables/arxiv/2607.10127v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 324}, {\"url\": \"assets/tables/arxiv/2607.10127v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1059, \"height\": 1344}]"
motivation: 现有LLM驱动的进化搜索受限于结构盲选、稀疏奖励和静态变异，导致搜索效率低且无法自适应。
method: 提出GAE，包含三个模块：GNN计算图嵌入、RL元控制器定向选择、在线GRPO微调LLM变异器。
result: 在非线性振荡器符号回归任务上，GAE发现精确闭式方程，性能匹配或超越静态基线，并达到SOTA分布外泛化。
conclusion: GAE将随机进化搜索转化为定向自改进过程，显著提升科学发现的效率和鲁棒性。
---

## 摘要
大语言模型（LLM）引导的进化程序搜索已成为自动化科学发现的一种强大范式。然而，当前方法受到三个瓶颈的根本限制：结构性盲目的父代选择、稀疏的整程序评估奖励以及无法在搜索过程中适应的静态变异算子。我们提出GAE（图增强进化）框架，通过紧密耦合的三支柱架构解决这些限制。首先，关系图神经网络（GNN）将程序解析为类型化计算图，生成结构感知嵌入。其次，RL优化的元控制器利用这些嵌入，将盲目的进化采样替换为有导向的策略，基于奖励历史动态选择最优父代和变异方向。第三，在线GRPO微调循环在测试时使用分组归一化评估奖励连续更新LLM变异算子，直接将模型的生成分布与高适应度结构编辑对齐。我们在一个具有挑战性的科学发现任务——复杂非线性振荡系统的符号回归上评估GAE。通过将随机搜索转化为有导向的、自我改进的轨迹，GAE高效地发现了闭式物理方程，持续匹配或超越静态LLM驱动的基线，并实现了最先进的分布外性能。

## Abstract
Evolutionary program search guided by Large Language Models (LLMs) has emerged as a powerful paradigm for automated scientific discovery. However, current approaches are fundamentally constrained by three bottlenecks: structurally blind parent selection, sparse whole-program evaluation rewards, and static mutation operators that fail to adapt during search. We present GAE (Graph-Augmented Evolution), a framework that resolves these limitations through a tightly coupled, three-pillar architecture. First, a relational graph neural network (GNN) parses programs into typed computation graphs, producing structure-aware embeddings. Second, an RL-optimized meta-controller leverages these embeddings to replace blind evolutionary sampling with a directed policy, dynamically selecting optimal parents and mutation directions based on reward history. Third, an online GRPO fine-tuning loop continuously updates the LLM mutation operator at test-time using group-normalized evaluation rewards, directly aligning the model's generation distribution with high-fitness structural edits. We evaluate GAE on a challenging scientific discovery task: symbolic regression for complex nonlinear oscillator systems. By transforming stochastic search into a directed, self-improving trajectory, GAE efficiently discovers closed-form physical equations, consistently matching or outperforming static LLM-driven baselines and achieving state-of-the-art out-of-distribution performance.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：大语言模型（LLM）引导的进化程序搜索在自动化科学发现中展现出潜力，但现有方法受三大瓶颈制约：  
  - **结构盲目父代选择**：仅基于适应度排名随机采样，忽略程序的结构信息，导致搜索缺乏局部相似性引导。  
  - **稀疏奖励**：整程序评估仅返回单个标量（如是否改进单元格），丢弃了部分正确、接近最优的中间结构信息。  
  - **静态变异算子**：LLM 权重在搜索过程中固定不变，无法根据累积经验调整生成分布。  
- **整体含义**：将进化搜索从随机探索转变为有导向、自适应的过程，对提高科学发现的效率和鲁棒性至关重要。

## 2. 论文提出的方法论

### 核心思想
GAE 在 OpenEvolve 框架上集成三个紧密耦合的组件：  
1. **关系图神经网络（GNN）编码器**：将程序的抽象语法树解析为类型化计算图，生成结构感知嵌入。  
2. **离散 Soft Actor-Critic（SAC）元控制器**：基于父代嵌入选择变异类型提示，实现定向变异。  
3. **在线 GRPO 微调循环**：使用分组归一化评估奖励实时更新 LLM 变异算子，使生成分布向高适应度结构对齐。  

### 关键技术细节
- **程序图表示**：静态 AST 分析，节点为数学操作（符号回归），边为数据流。失败时生成未知节点保证图有效。  
- **GNN 编码器**：  
  - 输入：节点类型 one-hot \( X \in \mathbb{R}^{N \times F} \)。  
  - 消息传递：\( L=2 \) 层关系图卷积，每层包含自环和按关系类型聚合的消息。  
  - 池化：平均池化和最大池化拼接后投影到 \( d=128 \) 维嵌入 \( z \)。  
  - 训练：预训练（合成数据 + 结构代理分数）→ 在线微调（每 3 代，使用最近 300 个程序的嵌入-奖励对，MSE 损失）。  
- **SAC 元控制器**：  
  - 状态：父代嵌入 \( z_\alpha \)。  
  - 动作空间：2700 个离散动作（5种变异类型 × 30个目标节点 × 18个参数索引）。  
  - 网络：三层 MLP（256维隐层），双 critic + 策略网络，熵自动调节。  
  - 奖励：\( r = \text{clip}(R(\beta)) + \lambda_1 \cdot \text{novelty} - \lambda_2 \cdot \text{complexity} \)，其中 novelty 基于嵌入余弦距离，complexity 基于 AST 节点数。  
  - 策略更新：TD 损失 + 熵正则化，使用优先级回放缓冲。  
- **GRPO 微调**：  
  - 每组生成 \( G=8 \) 个子代，计算组内相对优势 \( \hat{A}_g = (R_g - R_\alpha - \bar{R}^{\text{grp}}) / \text{std}(R^{\text{grp}}) \)。  
  - PPO-clip 目标 \( \mathcal{L}_{\text{GRPO}} \) 加 KL 惩罚（仅监控），更新全权重 LLM。  
- **算法流程**（Algorithm 1）：每一代均匀采样父代 → SAC 选择动作 → LLM 生成子代 → 评估 → GRPO 更新 LLM → SAC 更新（最优子代）→ 存档更新 → 周期性 GNN 微调。

## 3. 实验设计

- **任务**：非线性谐波振荡器的符号回归（预测加速度 \( \ddot{x} = dv/dt \)），需要发现闭式表达式。  
- **数据集**：LLM-SRBench 的 `phys_osc` 域（LSR-Synth 子集），包含训练集、ID 测试集、OOD 测试集（不同时间区域采样）。  
- **评估指标**：\( -\log_{10}(\text{NMSE}) \)（数值越低越好）。  
- **对比方法**：  
  - 非 LLM 基线：uDSR。  
  - LLM 引导进化：LLM-SR、OpenEvolve、CodeEvolve、ShinkaEvolve、PACEvolve（Single/Multi）。  
- **实验协议**：  
  - **高预算协议**：1000 迭代，10 次独立运行（表 1）。  
  - **低预算对比**：150 迭代，GAE 运行 3 次，PACEvolve 和 OpenEvolve 各 1 次（表 2）。  
- **LLM 主干**：GAE 使用 Qwen3.5-35B-A3B，基线中 PACEvolve 使用 Gemini 2.5 Flash，OpenEvolve 分别用 Qwen 和 Gemini。

## 4. 资源与算力

论文未明确说明使用的 GPU 型号、数量或训练时长。仅提及 LLM 为 Qwen3.5-35B-A3B（约 35B 参数，激活 3B），GRPO 学习率 \( 1\times10^{-6} \)，每代在线更新。未提供总计算量估计。

## 5. 实验数量与充分性

- **实验数量**：  
  - 表 1：高预算协议（8 种方法，10 次运行）。  
  - 表 2：低预算对比（3 种方法，GAE 3 次运行，其余 1 次）。  
  - 图 2：GAE 单次运行轨迹（150 迭代）。  
  - 附录展示了 GAE 发现的最优方程（式 10）及与其他方法的对比分析。  
- **充分性**：  
  - 正面：提供了与主流方法的对比，覆盖 ID 和 OOD 性能，低预算设置保证了计算公平性。  
  - 不足：  
    - 仅在一个任务（非线性振荡器）上评估，缺乏多任务泛化证据。  
    - 消融实验缺失（论文仅提及“禁用任意组件会退化到简单基线”，但未提供定量结果）。  
    - 统计显著性未明确（如置信区间或 t 检验）。  
    - 低预算对比中基线仅运行 1 次，可能受随机性影响。

## 6. 论文的主要结论与发现

- GAE 在 ID 和 OOD 测试集上均取得最低 NMSE：ID 平均 -6.87（最佳 -7.24），OOD 平均 -7.10（最佳 -7.45），显著优于 OpenEvolve 和 PACEvolve。  
- GAE 能够快速发现高质量闭式物理方程：在 150 迭代内，经过 13 次改进达到 NMSE ≈ \( 5.7\times10^{-8} \)。  
- 三个组件协同工作将随机搜索转化为定向自改进轨迹，有效克服了父代选择盲目、奖励稀疏和静态变异三大瓶颈。

## 7. 优点

- **方法创新性**：首次将 GNN 结构嵌入、RL 元控制器和在线 LLM 微调三者紧密耦合，同时解决三个独立问题。  
- **实验设计**：区分高预算和低预算协议，体现方法在计算受限场景下的优势。  
- **结果优越性**：在符号回归任务上达到 SOTA，尤其是 OOD 泛化能力突出（OOD NMSE 优于 ID，表明发现方程具有物理鲁棒性）。  
- **可解释性**：详细分析了每个方法发现方程中各物理项的含义（线性恢复力、阻尼、高阶非线性、相互作用项等），增强了科学可信度。  
- **框架可拆解**：三个组件参数独立、不同时间尺度运行，便于替换或禁用（附录 B）。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了非线性振荡器一个任务，未在更多科学领域（如天体物理、流体力学、生物学）验证。  
- **消融实验缺失**：未定量分析 GNN、SAC、GRPO 各自的贡献，缺乏组件重要性分析。  
- **统计鲁棒性不足**：低预算对比中基线仅运行 1 次，可能无法反映真实性能分布。  
- **计算资源未报告**：缺少 GPU 型号、数量、训练耗时，影响可重复性评估。  
- **领域适应性**：GNN 表示依赖 Python AST 的特定节点类型，迁移到其他语言或领域需重新设计，通用性受限。  
- **额外开销**：GNN 训练和 SAC 更新引入计算成本，仅在评估昂贵时才能体现净收益。

（完）
