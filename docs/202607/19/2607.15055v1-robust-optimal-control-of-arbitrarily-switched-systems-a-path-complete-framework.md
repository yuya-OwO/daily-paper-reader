---
title: "Robust Optimal Control of Arbitrarily Switched Systems: A Path-Complete Framework"
title_zh: 任意切换系统的鲁棒最优控制：一种路径完全框架
authors: "Léa Ninite, Adrien Banse, Guillaume O. Berger, Raphaël M. Jungers"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15055v1"
tags: ["query:lyapunov-ai"]
score: 8.0
evidence: 联合综合策略与类李雅普诺夫函数的上界认证，用于切换系统
tldr: 本文针对任意切换系统的鲁棒最优控制问题，提出一种基于路径完全图的框架，通过将函数关联到图节点并强制图基Bellman不等式，联合合成反馈策略及其无限时域闭环值函数的认证上界。该上界可被优化，并表示为图索引函数的逐点min-max组合。对于线性切换系统与二次阶段成本，推导出基于半正定规划和交替优化的可计算公式。建筑温度调节数值实验表明，该框架可直接用于反馈控制，也可用于模型预测控制中作为终端成本，有效提升性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15055v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 828, \"height\": 710, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15055v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 820, \"height\": 267, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15055v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 881, \"height\": 348, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15055v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1239, \"height\": 344, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15055v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1068, \"height\": 619, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15055v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 857, \"height\": 558, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15055v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 957, \"height\": 1280, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15055v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 909, \"height\": 1466, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15055v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 974, \"height\": 1167, \"label\": \"Table\"}]"
motivation: 现有切换系统鲁棒控制方法缺乏统一的性能认证框架，无法同时合成策略并给出可优化的性能上界。
method: 提出路径完全图框架，将函数关联到图节点，沿边施加Bellman不等式，利用可达图概念组合得到反馈策略与认证上界。
result: 在线性切换系统上得到基于半正定规划和交替优化的可计算公式，建筑温度调节实验验证了直接反馈控制和MPC中作为终端成本的有效性。
conclusion: 所提框架能同时输出可优化认证上界的反馈策略，兼具直接控制与MPC终端成本双重用途，具有实际应用价值。
---

## 摘要
本文研究具有性能保证的任意切换切换系统的鲁棒控制问题。我们提出一个框架，该框架联合综合反馈策略及其对应无限时域闭环值函数的认证上界。所提出的上界不仅能认证综合策略的性能，还能在控制器综合过程中进行优化。更准确地说，我们的方法将函数关联到路径完全图的节点，并沿其边施加基于图的贝尔曼不等式。利用新引入的可达性图概念，这些函数被组合成反馈策略及其对应闭环值函数的认证上界，该上界表示为图索引函数的逐点最小-最大组合。对于具有二次阶段成本的线性切换系统，所提出的框架基于半定规划和交替优化提供了易于处理的数值计算公式。数值实验，包括一个建筑温度调节基准测试，展示了所提方法在使用综合策略的直接反馈控制和使用认证上界作为终端代价的模型预测控制中的实际效用。

## Abstract
This paper addresses the robust control of switched systems under arbitrary switching with performance guarantees. We propose a framework that jointly synthesizes a feedback policy and a certified upper bound on its corresponding infinite-horizon closed-loop value function. The proposed upper bound not only certifies the performance of the synthesized policy, but can also be optimized during controller synthesis. More precisely, our approach associates functions with the nodes of a path-complete graph and enforces graph-based Bellman inequalities along its edges. Exploiting a newly introduced notion of reachability graph, these functions are combined into both a feedback policy and a certified upper bound on its corresponding closed-loop value function, expressed as a pointwise min-max combination of the graph-indexed functions. For linear switched systems with quadratic stage costs, the proposed framework admits tractable computational formulations based on semidefinite programming and alternating optimization. Numerical experiments, including a building temperature regulation benchmark, demonstrate the practical usefulness of the proposed approach both for direct feedback control using the synthesized policy and for model predictive control using the certified upper bound as a terminal cost.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：任意切换系统（模式序列不受控、可任意变化）的鲁棒最优控制极具挑战性。虽然可通过鲁棒动态规划（RDP）理论刻画最优值函数，但在连续状态/输入空间下，值函数常非光滑、非二次，甚至计算爆炸。现有方法如鲁棒模型预测控制（RMPC）虽能应对不确定性，但在线计算量随预测时域指数增长，且缺乏性能保证；鲁棒Lyapunov函数或屏障函数仅关注稳定性/安全性，未考虑最优性。
- **整体含义**：本文旨在同时解决两个问题——合成一个具体的反馈策略，并提供一个该策略对应无限时域闭环值函数的**认证上界**。这个上界既可作为控制器的性能证书，也可在综合过程中被优化（例如最小化某点的值），还可作为RMPC的终端代价函数，从而赋予RMPC无限时域性能保证。研究成果为切换系统提供了一类可计算、有保证的鲁棒最优控制方案。

### 2. 论文提出的方法论

- **核心思想**：
  1. **路径完全图**：利用文献[1]引入的路径完全图概念，将多个候选函数（如二次型）分配到图的节点上。图的结构保证了任意有限长模式序列都对应图中一条路径。
  2. **图基Bellman不等式**：沿图每条边 `(α,β,i)` 施加不等式：`V_α(x) ≥ c(x, π_A(x)) + V_β(f_i(x, π_A(x)))`，其中 `π_A` 是局部控制策略（由可达图节点索引），`V_α` 是与节点关联的函数。
  3. **可达图（Reachability Graph）**：新定义的概念，用于将图节点函数组合成最终的值函数上界 `V(x) = min_{A∈S_H} max_{α∈A} V_α(x)`，以及对应的反馈策略 `ϕ(x)=π_{A⋆(x)}(x)`，其中 `A⋆(x)` 选择使 `max_{α∈A} V_α(x)` 最小的节点集。
- **关键技术细节**：
  - **理论保证**：如果上述不等式成立，则 `V(x)` 满足鲁棒Bellman不等式（式(2)），从而成为策略 `ϕ` 的闭环值函数上界（Theorem 1）。
  - **特殊情形**：完整图（Complete Graph）上界退化为 `min_α V_α(x)`；共完整图（Co-complete Graph）上界退化为 `max_α V_α(x)`（Corollary 1, 2）。
  - **线性二次情形（LQR）**：假设线性动态、二次代价、线性状态反馈（`π_A(x)=K_A x`）、二次型函数 `V_α(x)=x^T P_α x`，则不等式化为矩阵不等式（PMI）：
    `P_α ≽ Q + K_A^T R K_A + (A_i+B_i K_A)^T P_β (A_i+B_i K_A)`。
  - **算法流程**：
    - **完整图情形**：可直接转化为线性矩阵不等式（LMI），通过一个SDP（式(18)）联合求解 `P_α` 和 `K_α`。
    - **非完整图（一般路径完全图）情形**：通过交替优化（Algorithm 1）求解：
      - 初始化：用稳定控制问题获得可行解。
      - P-step：固定 `K_A`，求解SDP（式(14)）优化 `P_α`（最小化 `trace(P_α)`）。
      - K-step：固定 `P_α`，通过二分法求最小 `γ`，使不等式成立，内部用Schur补转化为LMI求解 `K_A`（式(15)）。
      - 反复迭代直到收敛。

### 3. 实验设计

- **数据集/场景**：
  1. **合成二维例子**：一个2维线性切换系统（两个模式），用于可视化不同图结构下的值函数上界沿单位圆的变化。
  2. **建筑温度调节基准**：三区域建筑模型（源自[44]），3维状态（温度偏差），4个模式（由两个内部门的开闭组合产生）。控制目标是将温度调节到24°C参考值，初始偏差(5, -5, 5)°C。
- **Benchmark**：主要对比方法包括：
  - **鲁棒MPC（无终端代价）**：预测时域N=2~5，在线求解min-max问题，场景树包含所有可能模式序列。
  - **鲁棒MPC + 终端代价**：分别使用公共二次终端代价（单节点图获得）和本文一阶双德布鲁图终端代价。
  - **本文路径完全控制器**：使用**原德布鲁图（Primal De Bruijn）**（完整图，阶数ℓ=1~4，LMI求解）和**对偶德布鲁图（Dual De Bruijn）**（共完整图，阶数ℓ=1~4，交替优化求解）。
- **对比指标**：平均有限时域累积代价 `J_avg_T`（T=300步）、离线综合时间 `t_off`、平均在线执行时间 `t_avg_on`、总平均时间 `t_avg_tot = t_off + t_avg_on`。对路径完全控制器还报告认证上界 `V(x_0)`。

### 4. 资源与算力

- **文中未明确说明使用GPU**。所有实验在配备**Apple M5芯片和16GB RAM**的笔记本上运行，使用**Julia + JuMP + MOSEK**求解器。未报告训练时长（单次SDP或交替优化时间在表1中已给出，如离线时间0.04秒至55秒不等，无大规模GPU加速需求）。

### 5. 实验数量与充分性

- **实验数量**：
  - 合成例子：展示4种图阶数（ℓ=1~4）下原/对偶德布鲁图的值函数上界曲线（图5）。
  - 建筑温度调节：共比较**4类控制器 × 多种参数**：
    - 鲁棒MPC：N=2,3,4,5（4种无终端代价 + 3种有终端代价 = 7种配置）。
    - 路径完全控制器：原德布鲁图ℓ=1~4（4种） + 对偶德布鲁图ℓ=1~4（4种） = 8种配置。
    - 每个配置执行**50次蒙特卡洛仿真**（切换序列随机均匀采样，同一组序列用于所有控制器），结果取平均。
  - 缺乏消融研究：例如不同初始化对交替优化结果的影响、不同目标函数（如log-det）的对比等未做。
- **充分性与公平性**：
  - 实验较充分，覆盖了不同预测时域、不同图阶数，并展示了直接控制和MPC终端代价两种用法。
  - 公平性：同一组随机切换序列用于所有控制器；MPC与本文方法使用相同初始条件。
  - 局限性：基准模型规模小（3维状态），未验证高维或非线性系统；未与基于神经网络的近似动态规划等方法对比；未分析认证上界的保守性（与真实最优值函数的差距未知）。

### 6. 论文的主要结论与发现

1. **计算效率显著优于RMPC**：路径完全控制器的总计算时间（离线+在线）比RMPC低数个数量级，例如对偶德布鲁图ℓ=1控制器总时间1.51秒，而相同终端代价的RMPC（N=5）总时间1305秒。
2. **闭环性能接近甚至优于RMPC**：双德布鲁图ℓ=1控制器的平均代价1196.57，与RMPC+双德布鲁图终端代价（N=5）的1196.60几乎相同，且远低于RMPC无终端代价（N=5）的1486.71。
3. **所提上界作为MPC终端代价显著改善MPC性能**：加入终端代价后，MPC代价从1486.71（N=5，无终端）降至1196.60（N=5，双德布鲁图终端），并保证了递归最优代价递减。
4. **扩展至一般路径完全图的优势**：对偶（共完整）德布鲁图得到的性能优于原（完整）德布鲁图，验证了理论扩展的必要性。增加图阶数可改进性能，但双德布鲁图变化较小，存在饱和现象。
5. **合成例子**表明不同图结构在不同状态区域可能给出更紧的上界，凸显了路径完全图多样性的价值。

### 7. 优点

- **理论严谨性**：将鲁棒Bellman不等式与路径完全图深度融合，首次给出一般路径完全图下联合综合策略与认证上界的统一框架，并证明该上界满足鲁棒Bellman不等式（Theorem 1）。
- **计算可行性**：对线性二次情形导出LMI（完整图）和交替优化（通用图）可计算公式，避免了直接求解复杂值函数；算法无需状态空间离散化或神经网络训练。
- **实用性**：所产出的认证上界既可作为直接控制器的“证书”，又可作为RMPC的终端代价，实现无限时域保证；数值实验验证了其在实际工程（建筑温度调控）中的有效性。
- **全面性**：代码开源，方便复现和扩展。

### 8. 不足与局限

- **假设范围局限**：方法仅适用于**线性系统、二次代价、线性状态反馈**，对非线性系统或更一般代价（如分段线性）无法直接应用。
- **全局最优性无保证**：交替优化算法（Algorithm 1）依赖于初始化，可能收敛到局部最优，且不保证找到全局最小上界。
- **实验覆盖有限**：仅测试了3维系统，未验证高维扩展性；未考虑模式数量M很大的情况；未与基于深度强化学习或符号控制等方法公平比较。
- **认证保守性未知**：虽然上界是保证，但不清楚其与真实最优值函数之间的差距，缺乏保守性量化分析。
- **鲁棒性范围窄**：假设扰动仅源于任意切换，不包含加性噪声、参数不确定性或其他时变不确定性。

（完）
