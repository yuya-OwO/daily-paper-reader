---
title: Neural Symbollic Regression Using Deep Learning and Sparse Modelling
title_zh: 使用深度学习和稀疏建模的神经符号回归
authors: "Ravi Kumar U, Sumitra S"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.01102v1"
tags: ["query:sr"]
score: 9.0
evidence: 基于神经网络与稀疏建模的符号回归
tldr: 符号回归追求可解释表达式，但遗传规划可扩展性差、对噪声敏感，SINDy又依赖预设特征库。本文提出的NSR框架以神经网络作为函数预条件器，在交互感知的高维空间中学习平滑近似，再经LASSO稀疏建模得到闭式方程。Nguyen基准上，NSR在RMSE、噪声鲁棒性和分布外泛化上全面优于SINDy及未调参神经基线，消融实验确认了各模块的必要性。该工作弥合了神经逼近与符号发现，为科学机器学习提供了可规模化的可解释建模途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.01102v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 691, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01102v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 878, \"height\": 197, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01102v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 900, \"height\": 738, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01102v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 682, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01102v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 902, \"height\": 734, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01102v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 910, \"height\": 508, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.01102v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 930, \"height\": 326, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.01102v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 911, \"height\": 417, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.01102v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 962, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.01102v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 479, \"height\": 230, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.01102v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 874, \"height\": 158, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.01102v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 735, \"height\": 261, \"label\": \"Table\"}]"
motivation: 传统符号回归与SINDy分别受制于可扩展性差与依赖预设特征库，难以兼顾噪声鲁棒与表达式可解释。
method: 提出NSR：神经网络先学习交互感知空间中的平滑逼近，再用LASSO稀疏建模提取闭式表达式，并用Ray Tune/ASHA进行超参优化。
result: 在Nguyen基准上，NSR的RMSE、噪声鲁棒性和分布外泛化均优于SINDy与未调参神经基线，消融验证了设计有效性。
conclusion: 展示了从神经逼近到稀疏方程发现的可扩展可解释框架，为科学机器学习提供稳健的符号回归方案。
---

## 摘要
符号回归（SR）旨在找到简洁的数学表达式来表示数据中的基本关系，提供超越黑盒模型的可解释性和科学理解。然而，诸如遗传编程之类的传统方法面临可扩展性挑战且对噪声高度敏感，而诸如SINDy之类的稀疏回归技术则严重依赖预定的特征库。在这项工作中，我们提出了一种神经符号回归（NSR）框架，该框架将神经网络视为符号发现的功能性预处理器。我们的方法采用解耦流水线：神经网络首先在具有交互感知的非线性特征空间中学习目标函数的平滑、抗噪声近似。然后应用LASSO提取稀疏、可解释的闭式表达式。为了提高预测准确性和符号保真度，我们集成了分布式超参数优化与Ray Tune和ASHA调度。在Nguyen基准测试集上的实验表明，我们的方法在RMSE、噪声鲁棒性和分布外泛化方面始终优于SINDy和未调优的神经基线。消融研究证实了特征交互、神经深度和调优策略的重要性。总的来说，本研究提出了一个可扩展且可理解的神经符号框架，为科学机器学习中的神经近似与稀疏方程发现之间建立了坚实的联系。

## Abstract
Symbolic Regression (SR) seeks to find succinct mathematical expressions that represent the fundamental relationships within data, providing interpretability and scientific understanding that exceeds that of black-box models. Nevertheless, traditional methods like Genetic Programming face challenges with scalability and are highly sensitive to noise, while sparse regression techniques such as SINDy rely significantly on predetermined feature libraries. In this work, we present a Neural Symbolic Regression (NSR) framework that treats neural networks as functional preconditioners for symbolic discovery. Our approach uses a decoupled pipeline: a neural network first learns a smooth, noise-robust approximation of the target function in an interaction- aware nonlinear feature space. LASSO is then applied to extract sparse, interpretable closed-form expressions. To improve predictive accuracy and symbolic fidelity by integrating distributed hyperparameter optimization with Ray Tune and ASHA scheduling. Experiments on the Nguyen benchmark suite show that our approach consistently outperforms SINDy and non-tuned neural baselines in RMSE, noise robustness, and out-of-distribution generalization. Ablation studies confirm the significance of feature interactions, neural depth, and tuning strategies. In general, this study presents a scalable and understandable neural-symbolic framework, creating a solid link between neural approximation and the discovery of sparse equations for scientific machine learning.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 一、论文的核心问题与整体含义（研究动机与背景）

- **核心问题**：如何从含噪数据中发现可解释、具备泛化能力的闭式数学表达式，同时克服传统符号回归方法在可扩展性和噪声敏感性上的固有缺陷。
- **研究背景**：
  - 符号回归（SR）旨在自主发现数据背后的封闭解析公式，比黑盒模型更具可解释性和科学洞察力。
  - **遗传编程（GP）** 作为经典方法具有强表达力，但面临收敛慢、计算开销大、代码膨胀（code bloat）、在噪声或高维场景下受限等问题。
  - **SINDy** 等稀疏建模方法将符号回归重构为在预设特征库上的稀疏回归，绕开了组合搜索，但其表现取决于专家手工设计的特征库是否能涵盖目标函数的成分，缺乏捕捉复杂非线性交互的能力。
  - 新兴的**深度符号回归**（如 DSR）虽提升了灵活性，但常将函数逼近和符号发现耦合在单一优化流程中，导致训练不稳定、计算成本高且可解释性难以控制。
- **论文的核心立场**：提出一种全新的视角——将神经网络定位为**函数预条件器（functional preconditioner）**，而非符号的直接生成器；采用**解耦流水线**，先由神经网络在交互感知的特征空间中学习平滑、抗噪的近似，再由稀疏回归完成符号提取，以此兼顾鲁棒性、可扩展性与可解释性。

### 二、论文提出的方法论（NSR 框架）

#### 1. 核心思想
NSR 采用两阶段解耦策略：
- **阶段一（神经近似）**：MLP 在学习到的非线性特征空间中拟合目标函数，充当"平滑算子"，抑制观测噪声并稳定目标函数的潜在结构；
- **阶段二（稀疏提取）**：用 LASSO 对过完备特征库做稀疏回归，非零系数对应活跃的符号项，再用 SymPy 重建闭式表达式。

#### 2. 关键技术细节

**（1）非线性特征库构建**
- 基础变换集：F = {id, sin, cos, exp, log(1+x), x², x³, tanh}；
- 在基础库之上引入**二阶成对交互项**：Φ⁽²⁾ = {fₚ(xₐ)·f_q(x_b) | a ≠ b, fₚ, f_q ∈ F}；
- 全特征库：Φ_full = Φ(X) ∪ Φ⁽²⁾(X)，总维度 O(kd + k²·C(d,2))；
- 使用 Joblib 并行构建特征矩阵，并进行安全数值处理（如 NaN/Inf 替换）。

**（3）神经近似网络**
- 采用多层感知机（MLP），超参数（隐藏层宽度 H ∈ {32, 64, 128}，深度 L ∈ {1, 2, 3}）由调参确定；
- 激活函数使用 ReLU/GELU，配合 dropout 和权重衰减正则化，优化器为 AdamW；
- 训练损失：MSE + α‖θ‖²₂（L2 正则）；
- 使用 GPU（CUDA 或 Apple MPS）加速训练。

**（4）LASSO 稀疏符号提取**
- 通过 LASSO（L1 正则）获得稀疏系数向量 β*；
- 非零系数对应的特征即为活跃的符号项；利用稀疏恢复理论（RIP 条件）说明可辨识性依据；
- 使用 SymPy 将系数与符号项组合为解析表达式。

**（5）超参数自动优化**
- 集成 Ray Tune + ASHA 调度器，自动搜索学习率、网络宽度/深度、批大小、LASSO 正则强度等超参数空间；
- 以验证集 RMSE 为优化指标，ASHA 根据"中途指标中位数比较"机制提前终止低效试验。

#### 3. 算法流程（NSR Pipeline）
1. 输入数据 (X, y)；
2. 构建非线性特征 Φ(X)；
3. 构建二阶交互特征 Φ⁽²⁾(X)；
4. 合并为全特征矩阵 Φ_full；
5. 在 Φ_full 上训练神经网络 f_θ → y（平滑近似）；
6. 用 LASSO 提取稀疏符号系数 β；
7. 借助 SymPy 将活跃项转为解析表达式；
8. 输出符号函数 f*(x)。

### 三、实验设计

#### 1. 数据集 / Benchmark
- 采用 **Nguyen 符号回归基准套件**（Nguyen-1 至 Nguyen-7），覆盖多项式、三角函数、超越函数及混合类型表达式；
- 每个 benchmark 生成 n = 1000 个输入样本，均匀分布于 [−1, 1]^d；
- 输出叠加可选高斯噪声 εᵢ ~ N(0, 0.01²)；
- 数据划分：70% 训练、15% 验证、15% 测试。

#### 2. 对比方法
- **SINDy**：在同一特征库上进行线性稀疏回归；
- **非调参 Neural Baseline**：即 NSR 但跳过 Ray Tune 超参数优化；
- **PySR**：在 RMSE 对比表（表 IV）中额外作为参考（正文中提及 Ngueyn-4 上 PySR 更优，但整体稳定性不如 NSR）；
- **NSR（调参后）**：本文提出的完整框架。

#### 3. 评估维度
- **RMSE**（测试集）；
- **符号精度**（提取表达式与真实函数的接近程度）；
- **表达式复杂度**（LASSO 选中的活跃项数量）；
- **噪声鲁棒性**（不同 σ 下的表现）；
- **分布外泛化（OOD）**（训练域 [−1,1]，测试域 [−2,2]）。

### 四、资源与算力

- 论文涉及的硬件环境：
  - 文中明确提到实验运行于 Apple Silicon（MPS 后端）以及云 GPU 实例（NVIDIA T4 和 A100）；
  - 软件环境：PyTorch 2.0、scikit-learn 1.3、Ray Tune 2.x、SymPy、Joblib、Python 3.10。
- 运行时数据：表 III 给出平均运行时间对比：SINDy 约 1.2s、NSR Baseline 约 18.4s、PySR 约 24.6s、NSR Tuned 约 42.7s。
- 局限性说明：论文**并未清晰披露**每种实验条件下具体的 GPU 型号、GPU 数量、单次训练迭代次数或总实验时长等细化算力指标，仅提供了相对宏观的硬件描述和平均运行时间。

### 五、实验数量与充分性

#### 1. 实验覆盖
- 主实验：7 个 Nguyen 基准上的 RMSE 对比，但正文中表 IV 仅列出 Nguyen-1 ~ Nguyen-4 的定量结果，提及 Nguyen-5 ~ Nguyen-7 的内容较少；
- 补充实验：
  - 训练动态曲线（调参与未调参 NSR 的损失收敛对比）；
  - 符号方程恢复案例（Nguyen-3 上的典型示例）；
  - 预测质量散点图（Nguyen-5）；
  - 噪声鲁棒性实验（σ ∈ {0, 0.01, 0.05, 0.1}）；
  - 分布外泛化实验（Nguyen-4，[−1,1] 训练、[−2,2] 测试）。
- 消融实验（四个维度）：
  1. 特征库规模缩减（仅有纯多项式或纯三角函数）；
  2. 去除二阶交互项；
  3. 网络深度变化（L=1 浅层 vs L=2/3）；
  4. 去掉超参数优化（Ray Tune）。
  - 消融结果汇总于表 V（配置 → RMSE → 活跃项数）。

#### 2. 充分性与客观性分析
- **优点**：实验设置覆盖预测精度、符号保真性、噪声、OOD、计算时间、消融分析等多个层面，对框架的各个组件进行了系统性归因分析，具有一定的可信度；
- **不足**：
  - Nguyen benchmark 均为低维问题（d 较小），论文承认未验证高维真实科学数据集场景；
  - Ngueyn-5~7 的定量结果未给出，正文停留于定性描述，削弱了结论的完整性；
  - 随机种子固定但未说明重复次数细节；表 IV 声称"results are averaged over multiple runs"，但未给出方差/置信区间，统计显著性无从判断；
  - OOD 实验只展示了一个基准（Nguyen-4），代表性有限；
  - 与 PySR 的对比仅在 RMSE 层面，缺乏符号恢复精度、运行时间和噪声鲁棒性等多个维度的系统对照。

### 六、主要结论与发现

1. **NSR 在预测精度上全面占优**：在 Nguyen-1、Nguyen-2、Nguyen-3 上取得最低 RMSE（分别为 0.0087、0.0099、0.0122），明显优于 SINDy 和未调参神经基线；
2. **符号恢复质量高**：如 Nguyen-3 的真实函数 (x⁵+x⁴+x³+x²+x) 被恢复为 (1.002x⁵+0.998x⁴+1.001x³+1.000x²+0.999x)，系数误差极小；
3. **超参数调优贡献显著**：Ray Tune/ASHA 帮助模型更快收敛、达到更低损失，并发现更简洁准确的符号表达式；未经调参的 NSR 活跃项数偏多（7 vs 5）且 RMSE 更高（0.019 vs 0.004）；
4. **噪声鲁棒性优于 SINDy**：在各级噪声下性能退化更缓慢，证明了神经预条件器的去噪/平滑价值；
5. **OOD 泛化能力优于 SINDy**：在训练区间之外的测试域表现更稳定，SINDy 则出现明显发散；
6. **消融验证了各组件的必要性**：移除交互项、缩减特征库、使用浅层网络、去掉超参调优均使 RMSE 显著上升或符号保真度下降；
7. **整体方法定位**：NSR 建立了一个从"神经逼近能力"通向"稀疏符号发现"的桥梁，在准确性、可解释性和计算可行性之间达成了某种平衡。

### 七、优点

- **方法论创新性**：将神经网络角色重新定义为"函数预条件器"，与主流端到端神经符号回归形成明确对照，提出了新的设计哲学；
- **解耦式流水线设计**：神经逼近与符号提取分离，既降低了噪声传播，也便于模块独立改进和消融归因；
- **交互感知特征库**：在传统 SINDy 风格的多项式展开之外引入受控的二阶交互项，提升了跨维度关系的表达能力；
- **系统性集成超参数优化**：将 Ray Tune + ASHA 引入符号回归流水线，论文指出这是以往神经符号方法较少关注的维度，且实验证实显著提升符号恢复质量；
- **实验多维评估**：同时覆盖 RMSE、符号恢复可视化、噪声扫描、OOD、运行时对比、消融等，叙事结构完备；
- **复杂度分析清晰**：对特征构建、神经训练、LASSO 提取分别给出时间/空间复杂度，并与 GP、SINDy 并排比较（表 II），便于读者理解方法适用范围；
- **可复现性友好**：附录提供了核心代码片段、训练循环、LASSO 提取、Ray Tune 配置等细节，并声明使用固定随机种子。

### 八、不足与局限

1. **特征库组合爆炸**：二阶交互项和高维输入会导致特征数量呈组合级增长。论文文章自身在复杂度分析中也承认：全特征矩阵在数据集规模较大时可能难以直接存储；
2. **过度依赖平滑性假设**：NSR 要求被学习函数相对光滑。对于高度不连续或分段定义的函数，神经预条件器可能无法提供足以支撑符号恢复的表示；
3. **LASSO 自身偏差**：L1 正则化会系统性压缩系数（尤其当特征存在共线性时），导致提取的符号表达式在系数上与真实公式存在系统性偏差；
4. **计算开销偏高**：NSR-Tuned 平均运行时间约是 SINDy 的数倍甚至数十倍；在需要快速迭代或极大规模数据集的场景中会是一个瓶颈；
5. **实验验证范围有限**：仅使用低维 Nguyen 合成基准，缺少与真实世界科学数据（如物理过程、生物信号、气候/工程传感器）的结合；且对 Nguyen-5~7 缺乏定量报告；
6. **对比公平性存疑**：PySR 作为业界广泛使用的强基准，仅在单一 RMSE 榜单中对比，且未说明对应超参数设置或运行资源；SINDy 使用"与 NSR 相同特征库"的设置是否公平合理也值得商榷；
7. **统计严谨性不足**：多处报告均值但缺乏标准差、置信区间和显著性检验，难以从统计上判断优势是否可靠；
8. **消融实验的代表性不足**：消融用 Ngueyn-3 之外的具体 benchmark 数据占比小，可推断性在复杂、多元化函数空间中难以确证。

（完）
